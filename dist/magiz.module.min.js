var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __commonJS = (cb, mod) => function __require() {
  return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
  mod
));

// node_modules/math-expression-evaluator/dist/es/token.js
var require_token = __commonJS({
  "node_modules/math-expression-evaluator/dist/es/token.js"(exports) {
    "use strict";
    function e() {
      return e = Object.assign ? Object.assign.bind() : function(e2) {
        for (var t2 = 1; t2 < arguments.length; t2++) {
          var o2 = arguments[t2];
          for (var a in o2) Object.prototype.hasOwnProperty.call(o2, a) && (e2[a] = o2[a]);
        }
        return e2;
      }, e.apply(this, arguments);
    }
    Object.defineProperty(exports, "__esModule", { value: true });
    var t;
    var o = { 0: 11, 1: 0, 2: 3, 3: 0, 4: 0, 5: 0, 6: 0, 7: 11, 8: 11, 9: 1, 10: 10, 11: 0, 12: 11, 13: 0, 14: -1 };
    exports.tokenTypes = void 0, (t = exports.tokenTypes || (exports.tokenTypes = {}))[t.FUNCTION_WITH_ONE_ARG = 0] = "FUNCTION_WITH_ONE_ARG", t[t.NUMBER = 1] = "NUMBER", t[t.BINARY_OPERATOR_HIGH_PRECENDENCE = 2] = "BINARY_OPERATOR_HIGH_PRECENDENCE", t[t.CONSTANT = 3] = "CONSTANT", t[t.OPENING_PARENTHESIS = 4] = "OPENING_PARENTHESIS", t[t.CLOSING_PARENTHESIS = 5] = "CLOSING_PARENTHESIS", t[t.DECIMAL = 6] = "DECIMAL", t[t.POSTFIX_FUNCTION_WITH_ONE_ARG = 7] = "POSTFIX_FUNCTION_WITH_ONE_ARG", t[t.FUNCTION_WITH_N_ARGS = 8] = "FUNCTION_WITH_N_ARGS", t[t.BINARY_OPERATOR_LOW_PRECENDENCE = 9] = "BINARY_OPERATOR_LOW_PRECENDENCE", t[t.BINARY_OPERATOR_PERMUTATION = 10] = "BINARY_OPERATOR_PERMUTATION", t[t.COMMA = 11] = "COMMA", t[t.EVALUATED_FUNCTION = 12] = "EVALUATED_FUNCTION", t[t.EVALUATED_FUNCTION_PARAMETER = 13] = "EVALUATED_FUNCTION_PARAMETER", t[t.SPACE = 14] = "SPACE", exports.createTokens = function(t2) {
      return [{ token: "sin", show: "sin", type: 0, value: t2.math.sin }, { token: "cos", show: "cos", type: 0, value: t2.math.cos }, { token: "tan", show: "tan", type: 0, value: t2.math.tan }, { token: "pi", show: "&pi;", type: 3, value: "PI" }, { token: "(", show: "(", type: 4, value: "(" }, { token: ")", show: ")", type: 5, value: ")" }, { token: "P", show: "P", type: 10, value: t2.math.P }, { token: "C", show: "C", type: 10, value: t2.math.C }, { token: " ", show: " ", type: 14, value: " ".anchor }, { token: "asin", show: "asin", type: 0, value: t2.math.asin }, { token: "acos", show: "acos", type: 0, value: t2.math.acos }, { token: "atan", show: "atan", type: 0, value: t2.math.atan }, { token: "7", show: "7", type: 1, value: "7" }, { token: "8", show: "8", type: 1, value: "8" }, { token: "9", show: "9", type: 1, value: "9" }, { token: "int", show: "Int", type: 0, value: Math.floor }, { token: "cosh", show: "cosh", type: 0, value: t2.math.cosh }, { token: "acosh", show: "acosh", type: 0, value: t2.math.acosh }, { token: "ln", show: " ln", type: 0, value: Math.log }, { token: "^", show: "^", type: 10, value: Math.pow }, { token: "root", show: "root", type: 0, value: Math.sqrt }, { token: "4", show: "4", type: 1, value: "4" }, { token: "5", show: "5", type: 1, value: "5" }, { token: "6", show: "6", type: 1, value: "6" }, { token: "/", show: "&divide;", type: 2, value: t2.math.div }, { token: "!", show: "!", type: 7, value: t2.math.fact }, { token: "tanh", show: "tanh", type: 0, value: t2.math.tanh }, { token: "atanh", show: "atanh", type: 0, value: t2.math.atanh }, { token: "Mod", show: " Mod ", type: 2, value: t2.math.mod }, { token: "1", show: "1", type: 1, value: "1" }, { token: "2", show: "2", type: 1, value: "2" }, { token: "3", show: "3", type: 1, value: "3" }, { token: "*", show: "&times;", type: 2, value: t2.math.mul }, { token: "sinh", show: "sinh", type: 0, value: t2.math.sinh }, { token: "asinh", show: "asinh", type: 0, value: t2.math.asinh }, { token: "e", show: "e", type: 3, value: "E" }, { token: "log", show: " log", type: 0, value: t2.math.log }, { token: "0", show: "0", type: 1, value: "0" }, { token: ".", show: ".", type: 6, value: "." }, { token: "+", show: "+", type: 9, value: t2.math.add }, { token: "-", show: "-", type: 9, value: t2.math.sub }, { token: ",", show: ",", type: 11, value: "," }, { token: "Sigma", show: "&Sigma;", type: 12, value: t2.math.sigma }, { token: "n", show: "n", type: 13, value: "n" }, { token: "Pi", show: "&Pi;", type: 12, value: t2.math.Pi }, { token: "pow", show: "pow", type: 8, value: Math.pow, numberOfArguments: 2 }, { token: "&", show: "&", type: 9, value: t2.math.and }].map(function(t3) {
        return e({}, t3, { precedence: o[t3.type] });
      });
    }, exports.preced = o;
  }
});

// node_modules/math-expression-evaluator/dist/es/lexer.js
var require_lexer = __commonJS({
  "node_modules/math-expression-evaluator/dist/es/lexer.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var e = require_token();
    function t(e2, t2) {
      for (var n2 = 0; n2 < e2.length; n2++) e2[n2] += t2;
      return e2;
    }
    var n = { 0: true, 1: true, 3: true, 4: true, 6: true, 8: true, 9: true, 12: true, 13: true, 14: true };
    var s = { 0: true, 1: true, 2: true, 3: true, 4: true, 5: true, 6: true, 7: true, 8: true, 9: true, 10: true, 11: true, 12: true, 13: true };
    var o = { 0: true, 3: true, 4: true, 8: true, 12: true, 13: true };
    var p = {};
    var r = { 0: true, 1: true, 3: true, 4: true, 6: true, 8: true, 12: true, 13: true };
    var T = { 1: true };
    var h = [[], ["1", "2", "3", "7", "8", "9", "4", "5", "6", "+", "-", "*", "/", "(", ")", "^", "!", "P", "C", "e", "0", ".", ",", "n", " ", "&"], ["pi", "ln", "Pi"], ["sin", "cos", "tan", "Del", "int", "Mod", "log", "pow"], ["asin", "acos", "atan", "cosh", "root", "tanh", "sinh"], ["acosh", "atanh", "asinh", "Sigma"]];
    function E(e2, t2, n2, s2) {
      for (var o2 = 0; o2 < s2; o2++) if (e2[n2 + o2] !== t2[o2]) return false;
      return true;
    }
    function N(e2, t2) {
      for (var n2 = 0; n2 < t2.length; n2++) if (t2[n2].token === e2) return n2;
      return -1;
    }
    exports.addToken = function(t2) {
      for (var n2 = 0; n2 < t2.length; n2++) {
        var s2 = t2[n2].token.length, o2 = -1;
        t2[n2].type === e.tokenTypes.FUNCTION_WITH_N_ARGS && void 0 === t2[n2].numberOfArguments && (t2[n2].numberOfArguments = 2), h[s2] = h[s2] || [];
        for (var p2 = 0; p2 < h[s2].length; p2++) if (t2[n2].token === h[s2][p2]) {
          o2 = N(h[s2][p2], this.tokens);
          break;
        }
        -1 === o2 ? (this.tokens.push(t2[n2]), t2[n2].precedence = e.preced[t2[n2].type], h.length <= t2[n2].token.length && (h[t2[n2].token.length] = []), h[t2[n2].token.length].push(t2[n2].token)) : (this.tokens[o2] = t2[n2], t2[n2].precedence = e.preced[t2[n2].type]);
      }
    }, exports.lex = function(u, a) {
      var y, _ = { value: this.math.changeSign, type: e.tokenTypes.FUNCTION_WITH_ONE_ARG, precedence: 4, show: "-" }, i = { value: ")", show: ")", type: e.tokenTypes.CLOSING_PARENTHESIS, precedence: 0 }, A = { value: "(", type: e.tokenTypes.OPENING_PARENTHESIS, precedence: 0, show: "(" }, O = [A], R = [], l = u, k = n, I = 0, f = p, P = "";
      void 0 !== a && this.addToken(a);
      var c = function(e2, t2) {
        for (var n2, s2, o2, p2 = [], r2 = t2.length, T2 = 0; T2 < r2; T2++) if (!(T2 < r2 - 1 && " " === t2[T2] && " " === t2[T2 + 1])) {
          for (n2 = "", s2 = t2.length - T2 > h.length - 2 ? h.length - 1 : t2.length - T2; s2 > 0; s2--) if (void 0 !== h[s2]) for (o2 = 0; o2 < h[s2].length; o2++) E(t2, h[s2][o2], T2, s2) && (n2 = h[s2][o2], o2 = h[s2].length, s2 = 0);
          if (T2 += n2.length - 1, "" === n2) throw new Error("Can't understand after " + t2.slice(T2));
          p2.push(e2.tokens[N(n2, e2.tokens)]);
        }
        return p2;
      }(this, l);
      for (y = 0; y < c.length; y++) {
        var C = c[y];
        if (14 !== C.type) {
          var g, S = C.token, d = C.type, v2 = C.value, w = C.precedence, U = C.show, m = O[O.length - 1];
          for (g = R.length; g-- && 0 === R[g]; ) if (-1 !== [e.tokenTypes.FUNCTION_WITH_ONE_ARG, e.tokenTypes.BINARY_OPERATOR_HIGH_PRECENDENCE, e.tokenTypes.CONSTANT, e.tokenTypes.OPENING_PARENTHESIS, e.tokenTypes.CLOSING_PARENTHESIS, e.tokenTypes.BINARY_OPERATOR_LOW_PRECENDENCE, e.tokenTypes.BINARY_OPERATOR_PERMUTATION, e.tokenTypes.COMMA, e.tokenTypes.EVALUATED_FUNCTION, e.tokenTypes.EVALUATED_FUNCTION_PARAMETER].indexOf(d)) {
            if (true !== k[d]) throw new Error(S + " is not allowed after " + P);
            O.push(i), k = s, f = r, R.pop();
          }
          if (true !== k[d]) throw new Error(S + " is not allowed after " + P);
          true === f[d] && (d = e.tokenTypes.BINARY_OPERATOR_HIGH_PRECENDENCE, v2 = this.math.mul, U = "&times;", w = 3, y -= 1);
          var H = { value: v2, type: d, precedence: w, show: U, numberOfArguments: C.numberOfArguments };
          if (d === e.tokenTypes.FUNCTION_WITH_ONE_ARG) k = n, f = p, t(R, 1), O.push(H), c[y + 1].type !== e.tokenTypes.OPENING_PARENTHESIS && (O.push(A), R.push(2));
          else if (d === e.tokenTypes.NUMBER) m.type === e.tokenTypes.NUMBER ? (m.value += v2, t(R, 1)) : O.push(H), k = s, f = o;
          else if (d === e.tokenTypes.BINARY_OPERATOR_HIGH_PRECENDENCE) k = n, f = p, t(R, 2), O.push(H);
          else if (d === e.tokenTypes.CONSTANT) O.push(H), k = s, f = r;
          else if (d === e.tokenTypes.OPENING_PARENTHESIS) t(R, 1), I++, k = n, f = p, O.push(H);
          else if (d === e.tokenTypes.CLOSING_PARENTHESIS) {
            if (!I) throw new Error("Closing parenthesis are more than opening one, wait What!!!");
            I--, k = s, f = r, O.push(H), t(R, 1);
          } else if (d === e.tokenTypes.DECIMAL) {
            if (m.hasDec) throw new Error("Two decimals are not allowed in one number");
            m.type !== e.tokenTypes.NUMBER && (m = { show: "0", value: 0, type: e.tokenTypes.NUMBER, precedence: 0 }, O.push(m)), k = T, t(R, 1), f = p, m.value += v2, m.hasDec = true;
          } else d === e.tokenTypes.POSTFIX_FUNCTION_WITH_ONE_ARG && (k = s, f = r, t(R, 1), O.push(H));
          d === e.tokenTypes.FUNCTION_WITH_N_ARGS ? (k = n, f = p, t(R, C.numberOfArguments + 2), O.push(H), c[y + 1].type !== e.tokenTypes.OPENING_PARENTHESIS && (O.push(A), R.push(C.numberOfArguments + 2))) : d === e.tokenTypes.BINARY_OPERATOR_LOW_PRECENDENCE ? (m.type === e.tokenTypes.BINARY_OPERATOR_LOW_PRECENDENCE ? m.value === this.math.add ? (m.value = v2, m.show = U, t(R, 1)) : m.value === this.math.sub && "-" === U && (m.value = this.math.add, m.show = "+", t(R, 1)) : m.type !== e.tokenTypes.CLOSING_PARENTHESIS && m.type !== e.tokenTypes.POSTFIX_FUNCTION_WITH_ONE_ARG && m.type !== e.tokenTypes.NUMBER && m.type !== e.tokenTypes.CONSTANT && m.type !== e.tokenTypes.EVALUATED_FUNCTION_PARAMETER ? "-" === S && (k = n, f = p, t(R, 1).push(2), O.push(_), O.push(A)) : (O.push(H), t(R, 2)), k = n, f = p) : d === e.tokenTypes.BINARY_OPERATOR_PERMUTATION ? (k = n, f = p, t(R, 2), O.push(H)) : d === e.tokenTypes.COMMA ? (k = n, f = p, O.push(H)) : d === e.tokenTypes.EVALUATED_FUNCTION ? (k = n, f = p, t(R, 6), O.push(H), c[y + 1].type !== e.tokenTypes.OPENING_PARENTHESIS && (O.push(A), R.push(6))) : d === e.tokenTypes.EVALUATED_FUNCTION_PARAMETER && (k = s, f = r, O.push(H)), t(R, -1), P = S;
        } else if (y > 0 && y < c.length - 1 && 1 === c[y + 1].type && (1 === c[y - 1].type || 6 === c[y - 1].type)) throw new Error("Unexpected Space");
      }
      for (g = R.length; g--; ) O.push(i);
      if (true !== k[5]) throw new Error("complete the expression");
      for (; I--; ) O.push(i);
      return O.push(i), O;
    };
  }
});

// node_modules/math-expression-evaluator/dist/es/postfix.js
var require_postfix = __commonJS({
  "node_modules/math-expression-evaluator/dist/es/postfix.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true }), exports.toPostfix = function(e) {
      for (var p, t, u, l = [], o = -1, r = -1, s = [{ value: "(", type: 4, precedence: 0, show: "(" }], a = 1; a < e.length; a++) if (1 === e[a].type || 3 === e[a].type || 13 === e[a].type) 1 === e[a].type && (e[a].value = Number(e[a].value)), l.push(e[a]);
      else if (4 === e[a].type) s.push(e[a]);
      else if (5 === e[a].type) for (; 4 !== (null == (h = t = s.pop()) ? void 0 : h.type); ) {
        var h;
        t && l.push(t);
      }
      else if (11 === e[a].type) {
        for (; 4 !== (null == (v2 = t = s.pop()) ? void 0 : v2.type); ) {
          var v2;
          t && l.push(t);
        }
        s.push(t);
      } else {
        r = (p = e[a]).precedence, o = (u = s[s.length - 1]).precedence;
        var n = "Math.pow" == u.value && "Math.pow" == p.value;
        if (r > o) s.push(p);
        else {
          for (; o >= r && !n || n && r < o; ) t = s.pop(), u = s[s.length - 1], t && l.push(t), o = u.precedence, n = "Math.pow" == p.value && "Math.pow" == u.value;
          s.push(p);
        }
      }
      return l;
    };
  }
});

// node_modules/math-expression-evaluator/dist/es/postfix_evaluator.js
var require_postfix_evaluator = __commonJS({
  "node_modules/math-expression-evaluator/dist/es/postfix_evaluator.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true }), exports.postfixEval = function(e, a) {
      (a = a || {}).PI = Math.PI, a.E = Math.E;
      for (var p, u, r, l = [], s = void 0 !== a.n, t = 0; t < e.length; t++) if (1 === e[t].type) l.push({ value: e[t].value, type: 1 });
      else if (3 === e[t].type) l.push({ value: a[e[t].value], type: 1 });
      else if (0 === e[t].type) {
        var v2 = l[l.length - 1];
        Array.isArray(v2) ? v2.push(e[t]) : v2.value = e[t].value(v2.value);
      } else if (7 === e[t].type) {
        var y = l[l.length - 1];
        Array.isArray(y) ? y.push(e[t]) : y.value = e[t].value(y.value);
      } else if (8 === e[t].type) {
        for (var h = [], i = 0; i < e[t].numberOfArguments; i++) {
          var o = l.pop();
          o && h.push(o.value);
        }
        l.push({ type: 1, value: e[t].value.apply(e[t], h.reverse()) });
      } else if (10 === e[t].type) p = l.pop(), u = l.pop(), Array.isArray(u) ? ((u = u.concat(p)).push(e[t]), l.push(u)) : Array.isArray(p) ? (p.unshift(u), p.push(e[t]), l.push(p)) : l.push({ type: 1, value: e[t].value(u.value, p.value) });
      else if (2 === e[t].type || 9 === e[t].type) p = l.pop(), u = l.pop(), Array.isArray(u) ? ((u = u.concat(p)).push(e[t]), l.push(u)) : Array.isArray(p) ? (p.unshift(u), p.push(e[t]), l.push(p)) : l.push({ type: 1, value: e[t].value(u.value, p.value) });
      else if (12 === e[t].type) {
        p = l.pop();
        var n = void 0;
        n = !Array.isArray(p) && p ? [p] : p || [], u = l.pop(), r = l.pop(), l.push({ type: 1, value: e[t].value(r.value, u.value, n) });
      } else 13 === e[t].type && (s ? l.push({ value: a[e[t].value], type: 3 }) : l.push([e[t]]));
      if (l.length > 1) throw new Error("Uncaught Syntax error");
      return parseFloat(l[0].value.toFixed(15));
    };
  }
});

// node_modules/math-expression-evaluator/dist/es/functions.js
var require_functions = __commonJS({
  "node_modules/math-expression-evaluator/dist/es/functions.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.createMathFunctions = function(t) {
      return { isDegree: true, acos: function(n) {
        return t.math.isDegree ? 180 / Math.PI * Math.acos(n) : Math.acos(n);
      }, add: function(t2, n) {
        return t2 + n;
      }, asin: function(n) {
        return t.math.isDegree ? 180 / Math.PI * Math.asin(n) : Math.asin(n);
      }, atan: function(n) {
        return t.math.isDegree ? 180 / Math.PI * Math.atan(n) : Math.atan(n);
      }, acosh: function(t2) {
        return Math.log(t2 + Math.sqrt(t2 * t2 - 1));
      }, asinh: function(t2) {
        return Math.log(t2 + Math.sqrt(t2 * t2 + 1));
      }, atanh: function(t2) {
        return Math.log((1 + t2) / (1 - t2));
      }, C: function(n, r) {
        var a = 1, o = n - r, e = r;
        e < o && (e = o, o = r);
        for (var u = e + 1; u <= n; u++) a *= u;
        var i = t.math.fact(o);
        return "NaN" === i ? "NaN" : a / i;
      }, changeSign: function(t2) {
        return -t2;
      }, cos: function(n) {
        return t.math.isDegree && (n = t.math.toRadian(n)), Math.cos(n);
      }, cosh: function(t2) {
        return (Math.pow(Math.E, t2) + Math.pow(Math.E, -1 * t2)) / 2;
      }, div: function(t2, n) {
        return t2 / n;
      }, fact: function(t2) {
        if (t2 % 1 != 0) return "NaN";
        for (var n = 1, r = 2; r <= t2; r++) n *= r;
        return n;
      }, inverse: function(t2) {
        return 1 / t2;
      }, log: function(t2) {
        return Math.log(t2) / Math.log(10);
      }, mod: function(t2, n) {
        return t2 % n;
      }, mul: function(t2, n) {
        return t2 * n;
      }, P: function(t2, n) {
        for (var r = 1, a = Math.floor(t2) - Math.floor(n) + 1; a <= Math.floor(t2); a++) r *= a;
        return r;
      }, Pi: function(n, r, a) {
        for (var o = 1, e = n; e <= r; e++) o *= Number(t.postfixEval(a, { n: e }));
        return o;
      }, pow10x: function(t2) {
        for (var n = 1; t2--; ) n *= 10;
        return n;
      }, sigma: function(n, r, a) {
        for (var o = 0, e = n; e <= r; e++) o += Number(t.postfixEval(a, { n: e }));
        return o;
      }, sin: function(n) {
        return t.math.isDegree && (n = t.math.toRadian(n)), Math.sin(n);
      }, sinh: function(t2) {
        return (Math.pow(Math.E, t2) - Math.pow(Math.E, -1 * t2)) / 2;
      }, sub: function(t2, n) {
        return t2 - n;
      }, tan: function(n) {
        return t.math.isDegree && (n = t.math.toRadian(n)), Math.tan(n);
      }, tanh: function(n) {
        return t.math.sinh(n) / t.math.cosh(n);
      }, toRadian: function(t2) {
        return t2 * Math.PI / 180;
      }, and: function(t2, n) {
        return t2 & n;
      } };
    };
  }
});

// node_modules/math-expression-evaluator/dist/es/index.js
var require_es = __commonJS({
  "node_modules/math-expression-evaluator/dist/es/index.js"(exports, module) {
    "use strict";
    var t = require_lexer();
    var e = require_token();
    var i = require_postfix();
    var o = require_postfix_evaluator();
    var s = require_functions();
    var r = function() {
      function r2() {
        this.toPostfix = i.toPostfix, this.addToken = t.addToken, this.lex = t.lex, this.postfixEval = o.postfixEval, this.math = s.createMathFunctions(this), this.tokens = e.createTokens(this);
      }
      return r2.prototype.eval = function(t2, e2, i2) {
        return this.postfixEval(this.toPostfix(this.lex(t2, e2)), i2);
      }, r2;
    }();
    r.TOKEN_TYPES = e.tokenTypes, r.tokenTypes = e.tokenTypes, module.exports = r;
  }
});

// src/class/planClass/raw/_imports.ts
import { Matrix4, Matrix3, Vector2 } from "three";

// node_modules/three/src/extras/Earcut.js
var Earcut = {
  triangulate: function(data, holeIndices, dim = 2) {
    const hasHoles = holeIndices && holeIndices.length;
    const outerLen = hasHoles ? holeIndices[0] * dim : data.length;
    let outerNode = linkedList(data, 0, outerLen, dim, true);
    const triangles = [];
    if (!outerNode || outerNode.next === outerNode.prev) return triangles;
    let minX, minY, maxX, maxY, x, y, invSize;
    if (hasHoles) outerNode = eliminateHoles(data, holeIndices, outerNode, dim);
    if (data.length > 80 * dim) {
      minX = maxX = data[0];
      minY = maxY = data[1];
      for (let i = dim; i < outerLen; i += dim) {
        x = data[i];
        y = data[i + 1];
        if (x < minX) minX = x;
        if (y < minY) minY = y;
        if (x > maxX) maxX = x;
        if (y > maxY) maxY = y;
      }
      invSize = Math.max(maxX - minX, maxY - minY);
      invSize = invSize !== 0 ? 32767 / invSize : 0;
    }
    earcutLinked(outerNode, triangles, dim, minX, minY, invSize, 0);
    return triangles;
  }
};
function linkedList(data, start, end, dim, clockwise) {
  let i, last;
  if (clockwise === signedArea(data, start, end, dim) > 0) {
    for (i = start; i < end; i += dim) last = insertNode(i, data[i], data[i + 1], last);
  } else {
    for (i = end - dim; i >= start; i -= dim) last = insertNode(i, data[i], data[i + 1], last);
  }
  if (last && equals(last, last.next)) {
    removeNode(last);
    last = last.next;
  }
  return last;
}
function filterPoints(start, end) {
  if (!start) return start;
  if (!end) end = start;
  let p = start, again;
  do {
    again = false;
    if (!p.steiner && (equals(p, p.next) || area(p.prev, p, p.next) === 0)) {
      removeNode(p);
      p = end = p.prev;
      if (p === p.next) break;
      again = true;
    } else {
      p = p.next;
    }
  } while (again || p !== end);
  return end;
}
function earcutLinked(ear, triangles, dim, minX, minY, invSize, pass) {
  if (!ear) return;
  if (!pass && invSize) indexCurve(ear, minX, minY, invSize);
  let stop = ear, prev, next;
  while (ear.prev !== ear.next) {
    prev = ear.prev;
    next = ear.next;
    if (invSize ? isEarHashed(ear, minX, minY, invSize) : isEar(ear)) {
      triangles.push(prev.i / dim | 0);
      triangles.push(ear.i / dim | 0);
      triangles.push(next.i / dim | 0);
      removeNode(ear);
      ear = next.next;
      stop = next.next;
      continue;
    }
    ear = next;
    if (ear === stop) {
      if (!pass) {
        earcutLinked(filterPoints(ear), triangles, dim, minX, minY, invSize, 1);
      } else if (pass === 1) {
        ear = cureLocalIntersections(filterPoints(ear), triangles, dim);
        earcutLinked(ear, triangles, dim, minX, minY, invSize, 2);
      } else if (pass === 2) {
        splitEarcut(ear, triangles, dim, minX, minY, invSize);
      }
      break;
    }
  }
}
function isEar(ear) {
  const a = ear.prev, b = ear, c = ear.next;
  if (area(a, b, c) >= 0) return false;
  const ax = a.x, bx = b.x, cx = c.x, ay = a.y, by = b.y, cy = c.y;
  const x0 = ax < bx ? ax < cx ? ax : cx : bx < cx ? bx : cx, y0 = ay < by ? ay < cy ? ay : cy : by < cy ? by : cy, x1 = ax > bx ? ax > cx ? ax : cx : bx > cx ? bx : cx, y1 = ay > by ? ay > cy ? ay : cy : by > cy ? by : cy;
  let p = c.next;
  while (p !== a) {
    if (p.x >= x0 && p.x <= x1 && p.y >= y0 && p.y <= y1 && pointInTriangle(ax, ay, bx, by, cx, cy, p.x, p.y) && area(p.prev, p, p.next) >= 0) return false;
    p = p.next;
  }
  return true;
}
function isEarHashed(ear, minX, minY, invSize) {
  const a = ear.prev, b = ear, c = ear.next;
  if (area(a, b, c) >= 0) return false;
  const ax = a.x, bx = b.x, cx = c.x, ay = a.y, by = b.y, cy = c.y;
  const x0 = ax < bx ? ax < cx ? ax : cx : bx < cx ? bx : cx, y0 = ay < by ? ay < cy ? ay : cy : by < cy ? by : cy, x1 = ax > bx ? ax > cx ? ax : cx : bx > cx ? bx : cx, y1 = ay > by ? ay > cy ? ay : cy : by > cy ? by : cy;
  const minZ = zOrder(x0, y0, minX, minY, invSize), maxZ = zOrder(x1, y1, minX, minY, invSize);
  let p = ear.prevZ, n = ear.nextZ;
  while (p && p.z >= minZ && n && n.z <= maxZ) {
    if (p.x >= x0 && p.x <= x1 && p.y >= y0 && p.y <= y1 && p !== a && p !== c && pointInTriangle(ax, ay, bx, by, cx, cy, p.x, p.y) && area(p.prev, p, p.next) >= 0) return false;
    p = p.prevZ;
    if (n.x >= x0 && n.x <= x1 && n.y >= y0 && n.y <= y1 && n !== a && n !== c && pointInTriangle(ax, ay, bx, by, cx, cy, n.x, n.y) && area(n.prev, n, n.next) >= 0) return false;
    n = n.nextZ;
  }
  while (p && p.z >= minZ) {
    if (p.x >= x0 && p.x <= x1 && p.y >= y0 && p.y <= y1 && p !== a && p !== c && pointInTriangle(ax, ay, bx, by, cx, cy, p.x, p.y) && area(p.prev, p, p.next) >= 0) return false;
    p = p.prevZ;
  }
  while (n && n.z <= maxZ) {
    if (n.x >= x0 && n.x <= x1 && n.y >= y0 && n.y <= y1 && n !== a && n !== c && pointInTriangle(ax, ay, bx, by, cx, cy, n.x, n.y) && area(n.prev, n, n.next) >= 0) return false;
    n = n.nextZ;
  }
  return true;
}
function cureLocalIntersections(start, triangles, dim) {
  let p = start;
  do {
    const a = p.prev, b = p.next.next;
    if (!equals(a, b) && intersects(a, p, p.next, b) && locallyInside(a, b) && locallyInside(b, a)) {
      triangles.push(a.i / dim | 0);
      triangles.push(p.i / dim | 0);
      triangles.push(b.i / dim | 0);
      removeNode(p);
      removeNode(p.next);
      p = start = b;
    }
    p = p.next;
  } while (p !== start);
  return filterPoints(p);
}
function splitEarcut(start, triangles, dim, minX, minY, invSize) {
  let a = start;
  do {
    let b = a.next.next;
    while (b !== a.prev) {
      if (a.i !== b.i && isValidDiagonal(a, b)) {
        let c = splitPolygon(a, b);
        a = filterPoints(a, a.next);
        c = filterPoints(c, c.next);
        earcutLinked(a, triangles, dim, minX, minY, invSize, 0);
        earcutLinked(c, triangles, dim, minX, minY, invSize, 0);
        return;
      }
      b = b.next;
    }
    a = a.next;
  } while (a !== start);
}
function eliminateHoles(data, holeIndices, outerNode, dim) {
  const queue = [];
  let i, len, start, end, list;
  for (i = 0, len = holeIndices.length; i < len; i++) {
    start = holeIndices[i] * dim;
    end = i < len - 1 ? holeIndices[i + 1] * dim : data.length;
    list = linkedList(data, start, end, dim, false);
    if (list === list.next) list.steiner = true;
    queue.push(getLeftmost(list));
  }
  queue.sort(compareX);
  for (i = 0; i < queue.length; i++) {
    outerNode = eliminateHole(queue[i], outerNode);
  }
  return outerNode;
}
function compareX(a, b) {
  return a.x - b.x;
}
function eliminateHole(hole, outerNode) {
  const bridge = findHoleBridge(hole, outerNode);
  if (!bridge) {
    return outerNode;
  }
  const bridgeReverse = splitPolygon(bridge, hole);
  filterPoints(bridgeReverse, bridgeReverse.next);
  return filterPoints(bridge, bridge.next);
}
function findHoleBridge(hole, outerNode) {
  let p = outerNode, qx = -Infinity, m;
  const hx = hole.x, hy = hole.y;
  do {
    if (hy <= p.y && hy >= p.next.y && p.next.y !== p.y) {
      const x = p.x + (hy - p.y) * (p.next.x - p.x) / (p.next.y - p.y);
      if (x <= hx && x > qx) {
        qx = x;
        m = p.x < p.next.x ? p : p.next;
        if (x === hx) return m;
      }
    }
    p = p.next;
  } while (p !== outerNode);
  if (!m) return null;
  const stop = m, mx = m.x, my = m.y;
  let tanMin = Infinity, tan;
  p = m;
  do {
    if (hx >= p.x && p.x >= mx && hx !== p.x && pointInTriangle(hy < my ? hx : qx, hy, mx, my, hy < my ? qx : hx, hy, p.x, p.y)) {
      tan = Math.abs(hy - p.y) / (hx - p.x);
      if (locallyInside(p, hole) && (tan < tanMin || tan === tanMin && (p.x > m.x || p.x === m.x && sectorContainsSector(m, p)))) {
        m = p;
        tanMin = tan;
      }
    }
    p = p.next;
  } while (p !== stop);
  return m;
}
function sectorContainsSector(m, p) {
  return area(m.prev, m, p.prev) < 0 && area(p.next, m, m.next) < 0;
}
function indexCurve(start, minX, minY, invSize) {
  let p = start;
  do {
    if (p.z === 0) p.z = zOrder(p.x, p.y, minX, minY, invSize);
    p.prevZ = p.prev;
    p.nextZ = p.next;
    p = p.next;
  } while (p !== start);
  p.prevZ.nextZ = null;
  p.prevZ = null;
  sortLinked(p);
}
function sortLinked(list) {
  let i, p, q, e, tail, numMerges, pSize, qSize, inSize = 1;
  do {
    p = list;
    list = null;
    tail = null;
    numMerges = 0;
    while (p) {
      numMerges++;
      q = p;
      pSize = 0;
      for (i = 0; i < inSize; i++) {
        pSize++;
        q = q.nextZ;
        if (!q) break;
      }
      qSize = inSize;
      while (pSize > 0 || qSize > 0 && q) {
        if (pSize !== 0 && (qSize === 0 || !q || p.z <= q.z)) {
          e = p;
          p = p.nextZ;
          pSize--;
        } else {
          e = q;
          q = q.nextZ;
          qSize--;
        }
        if (tail) tail.nextZ = e;
        else list = e;
        e.prevZ = tail;
        tail = e;
      }
      p = q;
    }
    tail.nextZ = null;
    inSize *= 2;
  } while (numMerges > 1);
  return list;
}
function zOrder(x, y, minX, minY, invSize) {
  x = (x - minX) * invSize | 0;
  y = (y - minY) * invSize | 0;
  x = (x | x << 8) & 16711935;
  x = (x | x << 4) & 252645135;
  x = (x | x << 2) & 858993459;
  x = (x | x << 1) & 1431655765;
  y = (y | y << 8) & 16711935;
  y = (y | y << 4) & 252645135;
  y = (y | y << 2) & 858993459;
  y = (y | y << 1) & 1431655765;
  return x | y << 1;
}
function getLeftmost(start) {
  let p = start, leftmost = start;
  do {
    if (p.x < leftmost.x || p.x === leftmost.x && p.y < leftmost.y) leftmost = p;
    p = p.next;
  } while (p !== start);
  return leftmost;
}
function pointInTriangle(ax, ay, bx, by, cx, cy, px, py) {
  return (cx - px) * (ay - py) >= (ax - px) * (cy - py) && (ax - px) * (by - py) >= (bx - px) * (ay - py) && (bx - px) * (cy - py) >= (cx - px) * (by - py);
}
function isValidDiagonal(a, b) {
  return a.next.i !== b.i && a.prev.i !== b.i && !intersectsPolygon(a, b) && // doesn't intersect other edges
  (locallyInside(a, b) && locallyInside(b, a) && middleInside(a, b) && // locally visible
  (area(a.prev, a, b.prev) || area(a, b.prev, b)) || // does not create opposite-facing sectors
  equals(a, b) && area(a.prev, a, a.next) > 0 && area(b.prev, b, b.next) > 0);
}
function area(p, q, r) {
  return (q.y - p.y) * (r.x - q.x) - (q.x - p.x) * (r.y - q.y);
}
function equals(p1, p2) {
  return p1.x === p2.x && p1.y === p2.y;
}
function intersects(p1, q1, p2, q2) {
  const o1 = sign(area(p1, q1, p2));
  const o2 = sign(area(p1, q1, q2));
  const o3 = sign(area(p2, q2, p1));
  const o4 = sign(area(p2, q2, q1));
  if (o1 !== o2 && o3 !== o4) return true;
  if (o1 === 0 && onSegment(p1, p2, q1)) return true;
  if (o2 === 0 && onSegment(p1, q2, q1)) return true;
  if (o3 === 0 && onSegment(p2, p1, q2)) return true;
  if (o4 === 0 && onSegment(p2, q1, q2)) return true;
  return false;
}
function onSegment(p, q, r) {
  return q.x <= Math.max(p.x, r.x) && q.x >= Math.min(p.x, r.x) && q.y <= Math.max(p.y, r.y) && q.y >= Math.min(p.y, r.y);
}
function sign(num) {
  return num > 0 ? 1 : num < 0 ? -1 : 0;
}
function intersectsPolygon(a, b) {
  let p = a;
  do {
    if (p.i !== a.i && p.next.i !== a.i && p.i !== b.i && p.next.i !== b.i && intersects(p, p.next, a, b)) return true;
    p = p.next;
  } while (p !== a);
  return false;
}
function locallyInside(a, b) {
  return area(a.prev, a, a.next) < 0 ? area(a, b, a.next) >= 0 && area(a, a.prev, b) >= 0 : area(a, b, a.prev) < 0 || area(a, a.next, b) < 0;
}
function middleInside(a, b) {
  let p = a, inside = false;
  const px = (a.x + b.x) / 2, py = (a.y + b.y) / 2;
  do {
    if (p.y > py !== p.next.y > py && p.next.y !== p.y && px < (p.next.x - p.x) * (py - p.y) / (p.next.y - p.y) + p.x)
      inside = !inside;
    p = p.next;
  } while (p !== a);
  return inside;
}
function splitPolygon(a, b) {
  const a2 = new Node(a.i, a.x, a.y), b2 = new Node(b.i, b.x, b.y), an = a.next, bp = b.prev;
  a.next = b;
  b.prev = a;
  a2.next = an;
  an.prev = a2;
  b2.next = a2;
  a2.prev = b2;
  bp.next = b2;
  b2.prev = bp;
  return b2;
}
function insertNode(i, x, y, last) {
  const p = new Node(i, x, y);
  if (!last) {
    p.prev = p;
    p.next = p;
  } else {
    p.next = last.next;
    p.prev = last;
    last.next.prev = p;
    last.next = p;
  }
  return p;
}
function removeNode(p) {
  p.next.prev = p.prev;
  p.prev.next = p.next;
  if (p.prevZ) p.prevZ.nextZ = p.nextZ;
  if (p.nextZ) p.nextZ.prevZ = p.prevZ;
}
function Node(i, x, y) {
  this.i = i;
  this.x = x;
  this.y = y;
  this.prev = null;
  this.next = null;
  this.z = 0;
  this.prevZ = null;
  this.nextZ = null;
  this.steiner = false;
}
function signedArea(data, start, end, dim) {
  let sum = 0;
  for (let i = start, j = end - dim; i < end; i += dim) {
    sum += (data[j] - data[i]) * (data[i + 1] + data[j + 1]);
    j = i;
  }
  return sum;
}

// node_modules/three/src/extras/ShapeUtils.js
var ShapeUtils = class _ShapeUtils {
  // calculate area of the contour polygon
  static area(contour) {
    const n = contour.length;
    let a = 0;
    for (let p = n - 1, q = 0; q < n; p = q++) {
      a += contour[p].x * contour[q].y - contour[q].x * contour[p].y;
    }
    return a * 0.5;
  }
  static isClockWise(pts) {
    return _ShapeUtils.area(pts) < 0;
  }
  static triangulateShape(contour, holes) {
    const vertices = [];
    const holeIndices = [];
    const faces = [];
    removeDupEndPts(contour);
    addContour(vertices, contour);
    let holeIndex = contour.length;
    holes.forEach(removeDupEndPts);
    for (let i = 0; i < holes.length; i++) {
      holeIndices.push(holeIndex);
      holeIndex += holes[i].length;
      addContour(vertices, holes[i]);
    }
    const triangles = Earcut.triangulate(vertices, holeIndices);
    for (let i = 0; i < triangles.length; i += 3) {
      faces.push(triangles.slice(i, i + 3));
    }
    return faces;
  }
};
function removeDupEndPts(points) {
  const l = points.length;
  if (l > 2 && points[l - 1].equals(points[0])) {
    points.pop();
  }
}
function addContour(vertices, contour) {
  for (let i = 0; i < contour.length; i++) {
    vertices.push(contour[i].x);
    vertices.push(contour[i].y);
  }
}

// node_modules/three/src/math/MathUtils.js
var _seed = 1234567;
var DEG2RAD = Math.PI / 180;
var RAD2DEG = 180 / Math.PI;
function seededRandom(s) {
  if (s !== void 0) _seed = s;
  let t = _seed += 1831565813;
  t = Math.imul(t ^ t >>> 15, t | 1);
  t ^= t + Math.imul(t ^ t >>> 7, t | 61);
  return ((t ^ t >>> 14) >>> 0) / 4294967296;
}
function degToRad(degrees) {
  return degrees * DEG2RAD;
}

// src/class/planClass/raw/handleMath.ts
function rand100() {
  return Math.round(Math.random() * 1e3);
}
var v = rand100();
var seed = {
  /** 预设值，用于重置 */
  _v: v,
  /** 递增数值并返回 */
  get v() {
    v < Number.MAX_SAFE_INTEGER ? v++ : v = rand100();
    return v;
  },
  /** 重置为指定数值，或随机值 */
  set(x) {
    return v = this._v = x === "RANDOM" ? rand100() : x;
  },
  /** 重置递增数值为原数值 */
  reset() {
    return v = this._v;
  }
};
function sRand() {
  return seededRandom(seed.v);
}
function sSample(a) {
  let i = Math.floor(sRand() * a.length);
  return a[i < a.length ? i : 0];
}
function getBounds(points2D) {
  const pt = points2D?.[0];
  if (pt) {
    const min = pt.clone();
    const max = pt.clone();
    points2D.forEach((v2) => {
      const { x, y } = v2;
      x < min.x ? min.x = x : x > max.x ? max.x = x : 0;
      y < min.y ? min.y = y : y > max.y ? max.y = y : 0;
    });
    if (Math.round((max.x - min.x) * 1e6) > 0 && Math.round((max.y - min.y) * 1e6) > 0)
      return { min, max };
  }
  return void 0;
}
function isAlongAxis(r) {
  return r < 0.7854 || r > 5.4978 || 2.3562 < r && r < 3.927;
}
function crossLines(line1, line2) {
  const { x: x1, y: y1 } = line1.start;
  const { x: x2, y: y2 } = line1.end;
  const { x: x3, y: y3 } = line2.start;
  const { x: x4, y: y4 } = line2.end;
  const d = (x1 - x2) * (y3 - y4) - (y1 - y2) * (x3 - x4);
  if (d !== 0) {
    const t = ((x1 - x3) * (y3 - y4) - (y1 - y3) * (x3 - x4)) / d;
    const u = -((x1 - x2) * (y1 - y3) - (y1 - y2) * (x1 - x3)) / d;
    if (t >= 0 && t <= 1 && u >= 0 && u <= 1) {
      return new Vector2(x1 + t * (x2 - x1), y1 + t * (y2 - y1));
    }
  }
  return void 0;
}
function isPointInRect(point, rectangle) {
  const { x, y } = point;
  return x >= rectangle.min.x && x <= rectangle.max.x && y >= rectangle.min.y && y <= rectangle.max.y;
}
function lineInsideRect(line, rectangle) {
  const pointsInside = [line.start, line.end].filter((point) => isPointInRect(point, rectangle));
  const insideCount = pointsInside.length;
  if (insideCount === 2) {
    return { start: line.start.clone(), end: line.end.clone() };
  } else {
    const intersections = [];
    rectangle.lines.forEach((l) => {
      const i2 = crossLines(l, line);
      if (i2) intersections.push(i2);
    });
    const pt = pointsInside[0];
    const i = intersections[0];
    const i1 = intersections[1];
    if (pt) {
      if (i) {
        if (i && i1) {
          return getSameDirection(line, i, i1);
        } else {
          if (i.x !== pt.x || i.y !== pt.y) return getSameDirection(line, i, pt);
        }
      }
    } else if (i && i1 && (i.x !== i1.x || i.y !== i1.y)) {
      return getSameDirection(line, i, i1);
    }
  }
  return void 0;
}
function getSameDirection(line1, p1, p2) {
  const direction1 = { x: line1.end.x - line1.start.x, y: line1.end.y - line1.start.y };
  const direction2 = { x: p2.x - p1.x, y: p2.y - p1.y };
  return direction1.x * direction2.x + direction1.y * direction2.y > 0 ? { start: p1, end: p2 } : { start: p2, end: p1 };
}
function rayIntersectRay(ray1, ray2) {
  const { x: sx1, y: sy1 } = ray1.start;
  const { x: dx1, y: dy1 } = ray1.direction;
  const { x: sx2, y: sy2 } = ray2.start;
  const { x: dx2, y: dy2 } = ray2.direction;
  const r = dx1 * dy2 - dy1 * dx2;
  if (r !== 0) {
    const qmp = { x: sx2 - sx1, y: sy2 - sy1 };
    const t = (qmp.x * dy2 - qmp.y * dx2) / r;
    return new Vector2(sx1 + t * dx1, sy1 + t * dy1);
  }
  return;
}
function offsetRay(ray, offsetParams) {
  const { x, y } = ray.direction;
  const crossV = new Vector2(-y, x);
  crossV.setLength(isAlongAxis(crossV.angle()) ? offsetParams.x : offsetParams.y);
  const start = crossV.add(ray.start);
  return {
    start,
    end: start.clone().add(ray.direction),
    direction: ray.direction
  };
}
function matchRatioAndCount(spaces, distance, endWidth, sandwich) {
  const totalSpace = spaces.reduce((v2, s) => v2 + s, 0);
  if (totalSpace > 0) {
    const count = Math.round(distance / totalSpace);
    const total = totalSpace * count + (sandwich ? endWidth * 2 : endWidth);
    if (count > 0) return { ratio: distance / total, count };
  } else {
    console.error("total space for matching is 0");
  }
  return void 0;
}
function sweepPolygonX(y, lines) {
  const result = [];
  const a = [];
  lines.forEach((line) => {
    const i = pointAt(y, line);
    if (i) a.push(i);
  });
  a.sort((a2, b) => a2.x - b.x);
  if (a.length % 2 === 0)
    for (let i = 0; i < a.length; i += 2) {
      const a1 = a[i];
      const a2 = a[i + 1];
      result.push({ start: a1, end: a2 });
    }
  return result;
  function pointAt(y2, line) {
    const { x: sx, y: sy } = line.start;
    const { x: ex, y: ey } = line.end;
    if (sy < ey ? sy < y2 && y2 < ey : ey < y2 && y2 < sy) {
      const k = (ey - sy) / (ex - sx);
      const x = (y2 - sy) / k + sx;
      return new Vector2(x, y2);
    }
    return;
  }
}
function sRotateLinesAlong(rays, along) {
  let radian = 0;
  if (typeof along === "number") {
    radian = along * (Math.PI / 180);
  } else if (along === "RANDOM") {
    radian = Math.PI * 2 * sRand();
  } else if (along === "DEPTH") {
    radian = Math.PI / 2;
  } else if (along === "WIDTH") {
    radian = 0;
  } else if (along === "LONGEST") {
    const r = rays.reduce((a, b) => a.direction.length() > b.direction.length() ? a : b);
    radian = r.direction.angle();
  } else if (along === "SHORTEST") {
    const r = rays.reduce((a, b) => a.direction.length() < b.direction.length() ? a : b);
    radian = r.direction.angle();
  }
  if (radian) {
    const matrix = new Matrix3().makeRotation(radian);
    rays = rays.map((r) => {
      return {
        direction: r.direction.clone().applyMatrix3(matrix),
        start: r.start.clone().applyMatrix3(matrix),
        end: r.end.clone().applyMatrix3(matrix)
      };
    });
  } else {
    rays = rays.map((r) => {
      return {
        direction: r.direction.clone(),
        start: r.start.clone(),
        end: r.end.clone()
      };
    });
  }
  return { newRays: rays, radian };
}
function getClampedRects(bounds, params) {
  const min = bounds.min.clone();
  const max = bounds.max.clone();
  const w = max.x - min.x;
  const h = max.y - min.y;
  const { startX, endX, startY, endY, asRatio, reverse } = params;
  const result = [{ min, max }];
  setClamped(result, w, "x", startX, endX, asRatio, reverse);
  setClamped(result, h, "y", startY, endY, asRatio, reverse);
  return result;
}
function setClamped(bounds, distance, axis, start, end, asRatio, reverse) {
  if (start || end) {
    const dStart = start ? asRatio ? start * distance : start : 0;
    const dEnd = end ? asRatio ? end * distance : end : 0;
    bounds.forEach((rect) => {
      if (reverse) {
        if (dStart && dEnd) {
          const newRect = {
            min: rect.min.clone(),
            max: rect.max.clone()
          };
          bounds.push(newRect);
          newRect.min[axis] = newRect.max[axis] - dEnd;
          rect.max[axis] = rect.min[axis] + dStart;
        } else if (dStart) {
          rect.max[axis] = rect.min[axis] + dStart;
        } else if (dEnd) {
          rect.min[axis] = rect.max[axis] - dEnd;
        }
      } else {
        rect.min[axis] += dStart;
        rect.max[axis] -= dEnd;
      }
    });
  }
}
function getLoopNext(a, i) {
  let nextID = i + 1;
  if (nextID >= a.length) nextID = 0;
  return a[nextID];
}
function getValidIndexes(count, control) {
  let result = [];
  if (control) {
    const { total, indent, filter, chance } = control;
    if (total > 0) count = total;
    if (indent) {
      indentIndexes(count, indent, result);
    } else {
      for (let i = 0; i < count; i++) result.push(i);
    }
    const filterSteps = filter?.reduce((v2, f) => v2 + Math.abs(f), 0) || 0;
    if (filter && filterSteps > 0) {
      result = result.filter((_, i) => {
        let fid = i % filterSteps;
        const found = filter.find((f) => {
          const step = Math.abs(f);
          if (fid < step) {
            return true;
          } else {
            fid -= step;
            return false;
          }
        });
        return !found || found > 0;
      });
    }
    if (chance > 0) {
      result = result.filter(() => sRand() < chance);
    }
  } else {
    for (let i = 0; i < count; i++) result.push(i);
  }
  return result;
}
function indentIndexes(total, indent, result) {
  let startID = 0;
  let endID = total - 1;
  const { central, asRatio, reverse, start, end } = indent;
  if (start) {
    const dStart = asRatio ? Math.round(total * start) : start;
    startID = central ? Math.round((total - 1) / 2 - dStart) : dStart;
  }
  if (end) {
    const dEnd = asRatio ? Math.round(total * end) : end;
    endID = central ? Math.round((total - 1) / 2 + dEnd) : total - 1 - dEnd;
  }
  if (reverse) {
    for (let i = 0; i < startID; i++) result.push(i);
    for (let i = endID + 1; i < total; i++) result.push(i);
  } else {
    for (let i = startID; i <= endID; i++) result.push(i);
  }
}

// src/class/planClass/raw/handleBox.ts
var TEMP = new Matrix4();
function edgeUnitToTempBoxes(params, xRatio) {
  const result = [];
  boxesToTempBoxes(result, params.boxes, xRatio);
  flexVerticalsToTempBoxes(result, params.flexes, xRatio);
  return result;
}
function boxesToTempBoxes(result, params, xRatio) {
  params.forEach((boxParams) => {
    let { widthX, depthY, heightZ } = boxParams;
    if (widthX || depthY || heightZ) {
      const matrix = new Matrix4().makeTranslation(0, 0, 0.5);
      if (widthX < 0) widthX = -widthX;
      if (depthY < 0) depthY = -depthY;
      if (heightZ < 0) {
        matrix.premultiply(TEMP.makeTranslation(0, 0, -1));
        heightZ = -heightZ;
      }
      matrix.premultiply(TEMP.makeScale(widthX * xRatio, depthY, heightZ));
      applyBasicTransform(boxParams, matrix);
      result.push({ matrix, color: boxParams.color });
    }
  });
}
function flexVerticalsToTempBoxes(result, params, xRatio) {
  params.forEach((flexParams) => {
    const { unitHeight, flexHeight, flexDepth, flexWidth, dash, shrink, seg } = flexParams;
    const flexResult = getFlexResult(flexHeight, [unitHeight], seg, false, dash, shrink);
    flexResult.forEach((u) => {
      const matrix = new Matrix4().makeTranslation(0, 0, 0.5).premultiply(TEMP.makeScale(flexWidth * xRatio, flexDepth, u.space));
      applyBasicTransform(flexParams, matrix);
      matrix.premultiply(TEMP.makeTranslation(0, 0, u.move));
      result.push({ matrix, color: flexParams.color });
    });
  });
}
function edgeFlexToTempBoxes(params, distance) {
  const result = [];
  const { flexDepth, flexHeight, array, seg, sandwich, ctrlFlex, shrink } = params;
  const flexResult = getFlexResult(distance, array, seg, sandwich, ctrlFlex, shrink);
  flexResult.forEach((u) => {
    const matrix = new Matrix4().makeTranslation(0.5, 0, flexHeight > 0 ? 0.5 : -0.5).premultiply(TEMP.makeScale(u.space, Math.abs(flexDepth), Math.abs(flexHeight)));
    applyBasicTransform(params, matrix);
    matrix.premultiply(TEMP.makeTranslation(u.move, 0, 0));
    result.push({ matrix, color: params.color });
  });
  return result;
}
function matchUnitsToTempBoxRows(params, lines, sandwich, simplify) {
  function pushSweep(y, saveAs, lines2, data) {
    const { depth, matchUnit } = data;
    y += data.depth / 2;
    saveAs.push(
      sweepPolygonX(y, lines2).map((pair) => {
        return Object.assign(pair, {
          depth,
          matchUnit,
          statusJSON: JSON.stringify(matchUnit.trans) + JSON.stringify(matchUnit.color)
        });
      })
    );
    return y + depth / 2;
  }
  const result = [];
  const bounds = getBounds(lines.map((line) => line.start));
  if (bounds) {
    const depths = [];
    params.forEach((matchUnit) => {
      for (let i = 0; i < matchUnit.count; i++) depths.push(matchUnit.unitDepth);
    });
    const rc = matchRatioAndCount(depths, bounds.max.y - bounds.min.y, depths[0], sandwich);
    if (rc) {
      const depthData = [];
      params.forEach((matchUnit) => {
        for (let i = 0; i < matchUnit.count; i++) {
          depthData.push({ matchUnit, depth: matchUnit.unitDepth * rc.ratio });
        }
      });
      let rowsSweepX = [];
      let y = bounds.min.y;
      for (let i = 0; i < rc.count; i++) {
        depthData.forEach((data) => {
          y = pushSweep(y, rowsSweepX, lines, data);
        });
      }
      if (sandwich) {
        pushSweep(y, rowsSweepX, lines, depthData[0]);
      }
      if (simplify) rowsSweepX = simplifySweepX(rowsSweepX);
      rowsSweepX.forEach((row) => {
        const newRow = [];
        row.map((line) => {
          const { depth, start, end } = line;
          const { flexHeight, color, shrink } = line.matchUnit;
          const mtx = new Matrix4().makeTranslation(0.5, 0, flexHeight > 0 ? 0.5 : -0.5).premultiply(TEMP.makeScale(1, depth, Math.abs(flexHeight)));
          indentBoxFlexWidth(shrink, Math.abs(start.x - end.x), mtx).forEach((matrix) => {
            applyBasicTransform(line.matchUnit, matrix);
            matrix.premultiply(TEMP.makeTranslation(start.x, start.y, 0));
            newRow.push({ matrix, color });
          });
        });
        result.push(newRow);
      });
    }
  }
  return result;
}
function simplifySweepX(rowsSweepX) {
  function isSame(a, b) {
    const precision = 6;
    return a.toFixed(precision) === b.toFixed(precision);
  }
  const result = [];
  rowsSweepX.forEach((row, i) => {
    if (i === 0) {
      result.push(row);
    } else {
      const lastRow = result[result.length - 1];
      row.forEach((line) => {
        const thisRow = [];
        const same = lastRow.find(
          (lastLine) => isSame(lastLine.start.x, line.start.x) && isSame(lastLine.end.x, line.end.x) && isSame(lastLine.matchUnit.flexHeight, line.matchUnit.flexHeight) && lastLine.statusJSON === line.statusJSON
        );
        if (same) {
          const moveY = line.depth / 2;
          same.start.y += moveY;
          same.end.y += moveY;
          same.depth += line.depth;
        } else {
          thisRow.push(line);
        }
        if (thisRow.length > 0) result.push(thisRow);
      });
    }
  });
  return result;
}
function pushBoxData(saveAs, color, matrix) {
  const { index, glass } = sSample(color);
  const target = saveAs.instanced[glass ? "boxGlass" : "box"];
  target.colors.push(index);
  target.matrices.push(matrix.toArray());
}
function indentBoxFlexWidth(params, width, matrix) {
  const result = [];
  if (params) {
    const { central, asRatio, reverse, start, end } = params;
    const wStart = asRatio ? width * start : start;
    const wEnd = asRatio ? width * end : end;
    if (reverse) {
      if (start) {
        const w = central ? width / 2 - wStart : wStart;
        if (w > 0) result.push(matrix.clone().premultiply(TEMP.makeScale(w, 1, 1)));
      }
      if (end < width - start) {
        const w = central ? width / 2 - wEnd : wEnd;
        const x = central ? width / 2 + wEnd : width - wEnd;
        if (w > 0)
          result.push(
            matrix.clone().premultiply(TEMP.makeScale(w, 1, 1)).premultiply(TEMP.makeTranslation(x, 0, 0))
          );
      }
    } else {
      const w = central ? wStart + wEnd : width - wStart - wEnd;
      if (w > 0) {
        matrix.premultiply(TEMP.makeScale(w, 1, 1));
        if (start) {
          matrix.premultiply(TEMP.makeTranslation(central ? width / 2 - wStart : wStart, 0, 0));
        }
        result.push(matrix);
      }
    }
  } else {
    result.push(matrix.premultiply(TEMP.makeScale(width, 1, 1)));
  }
  return result;
}
function applyBasicTransform(status, matrix) {
  status.trans?.forEach((transform) => {
    if ("rotateX" in transform) {
      matrix.premultiply(TEMP.makeRotationX(degToRad(transform.rotateX)));
    } else if ("rotateY" in transform) {
      matrix.premultiply(TEMP.makeRotationY(degToRad(transform.rotateY)));
    } else if ("rotateZ" in transform) {
      matrix.premultiply(TEMP.makeRotationZ(degToRad(transform.rotateZ)));
    } else {
      matrix.premultiply(TEMP.makeTranslation(transform.moveX, transform.moveY, transform.moveZ));
    }
  });
}
function getFlexResult(distance, spaceArray, asSegments, sandwich, control, shrink) {
  const flexUnits = [];
  const rc = matchRatioAndCount(spaceArray, distance, sandwich ? spaceArray[0] : 0, sandwich);
  if (rc) {
    const validIndexes = getValidIndexes(rc.count * spaceArray.length, control);
    const totalCount = rc.count * spaceArray.length;
    const spacesScaled = spaceArray.map((s) => s * rc.ratio);
    let move = 0;
    let lastValidIndex = -1;
    for (let i = 0; i < totalCount; i++) {
      const space = spacesScaled[i % spaceArray.length];
      lastValidIndex = handleEdgeFlex(
        lastValidIndex,
        i,
        move,
        space,
        asSegments,
        validIndexes,
        flexUnits
      );
      move += space;
    }
    if (sandwich && validIndexes[0] === 0) {
      handleEdgeFlex(
        lastValidIndex,
        totalCount,
        move,
        spacesScaled[0],
        asSegments,
        [totalCount],
        flexUnits
      );
    }
  }
  if (shrink) {
    const result = [];
    const { start, end, asRatio, central, reverse } = shrink;
    flexUnits.forEach((u) => {
      if (reverse) {
        if (central) {
          if (start) {
            const startD = u.space / 2 - (asRatio ? u.space * start : start);
            u.move += startD;
            u.space -= startD;
          }
          if (end) {
            const endD = u.space / 2 - (asRatio ? u.space * end : end);
            u.space -= endD;
          }
          result.push(u);
        } else {
          if (start) {
            result.push({
              move: u.move,
              space: asRatio ? u.space * start : start
            });
          }
          if (end) {
            const space = asRatio ? u.space * end : end;
            result.push({ move: u.move + u.space - space, space });
          }
        }
      } else {
        if (central) {
          const half = u.space / 2;
          const startPart = { move: u.move, space: half };
          const endPart = { move: u.move + half, space: half };
          if (start) {
            startPart.space -= asRatio ? u.space * start : start;
          }
          if (end) {
            const endD = asRatio ? u.space * end : end;
            endPart.space -= endD;
            endPart.move += endD;
          }
          result.push(startPart);
          result.push(endPart);
        } else {
          if (start) {
            const startD = asRatio ? u.space * start : start;
            u.move += startD;
            u.space -= startD;
          }
          if (end) {
            const endD = asRatio ? u.space * end : end;
            u.space -= endD;
          }
          result.push(u);
        }
      }
    });
    return result.filter((r) => r.space > 0);
  } else {
    return flexUnits;
  }
}
function handleEdgeFlex(lastValidIndex, i, move, space, asSegments, validIndexes, flexUnits) {
  if (validIndexes.find((vi) => vi === i) !== void 0) {
    if (flexUnits.length === 0) {
      flexUnits.push({ move, space });
    } else {
      const thisUnit = flexUnits[flexUnits.length - 1];
      if (!asSegments && lastValidIndex === i - 1) {
        thisUnit.space += space;
      } else {
        flexUnits.push({ move, space });
      }
    }
    lastValidIndex = i;
  }
  return lastValidIndex;
}

// src/class/planClass/raw/others.ts
function handleBoundingBox(result, parsedStyle, bounds) {
  const { boundingBox, elevations } = parsedStyle;
  boundingBox.forEach((params) => {
    const { color, height, clamp } = params;
    const rects = clamp ? getClampedRects(bounds, clamp) : [bounds];
    rects.forEach((rect) => {
      const { min, max } = rect;
      const mtx = new Matrix4().makeTranslation(0.5, 0.5, 0.5);
      if (height < 0) mtx.premultiply(TEMP.makeTranslation(0, 0, -1));
      mtx.premultiply(TEMP.makeScale(max.x - min.x, max.y - min.y, Math.abs(height)));
      applyBasicTransform(params, mtx);
      elevations.forEach((elevation) => {
        pushBoxData(result, color, mtx.premultiply(TEMP.makeTranslation(min.x, min.y, elevation)));
      });
    });
  });
}
function handleSlopingRoof(result, parsedStyle, bounds) {
  const { slopingRoof, elevations } = parsedStyle;
  slopingRoof.forEach((params) => {
    const { form, height, overhang } = params;
    const { min, max } = bounds;
    const mtx = new Matrix4().makeScale(
      max.x - min.x + overhang * 2,
      max.y - min.y + overhang * 2,
      Math.abs(height)
    );
    applyBasicTransform(params, mtx);
    mtx.premultiply(TEMP.makeTranslation(min.x - overhang, min.y - overhang, 0));
    const { index, glass } = sSample(params.color);
    const target = result.instanced[form === "2" ? glass ? "slope2Glass" : "slope2" : glass ? "slope4Glass" : "slope4"];
    target.colors.push(index);
    elevations.forEach((elevation) => {
      target.matrices.push(mtx.premultiply(TEMP.makeTranslation(0, 0, elevation)).toArray());
    });
  });
}
function handleAppendent(result, parsedStyle, rayLoops) {
  const { appendent, elevations } = parsedStyle;
  appendent.forEach((params) => {
    const { boxes, place, count } = params;
    const isOnEdge = place === "EDGE";
    for (let i = 0; i < count; i++) {
      const point = isOnEdge ? randomPointOnEdge(rayLoops) : randomPointInPolygon(rayLoops);
      if (point) {
        boxes.forEach((box) => {
          const mtx = new Matrix4().makeTranslation(0, 0, 0.5).premultiply(TEMP.makeScale(box.widthX, box.depthY, box.heightZ));
          applyBasicTransform(box, mtx);
          elevations.forEach((elevation) => {
            pushBoxData(
              result,
              box.color,
              mtx.premultiply(TEMP.makeTranslation(point.x, point.y, elevation))
            );
          });
        });
      }
    }
  });
}
function randomPointInPolygon(loops) {
  const rangeY = { min: Infinity, max: -Infinity };
  loops[0]?.forEach((p) => {
    rangeY.min = Math.min(rangeY.min, p.start.y);
    rangeY.max = Math.max(rangeY.max, p.start.y);
  });
  const randomY = rangeY.min + sRand() * (rangeY.max - rangeY.min);
  const sweepX = sweepPolygonX(randomY, loops.flat());
  const pair = sweepX[Math.round(sRand() * (sweepX.length - 1))];
  return pair ? new Vector2(pair.start.x + (pair.end.x - pair.start.x) * sRand(), randomY) : void 0;
}
function randomPointOnEdge(loops) {
  const loop = loops[0];
  if (loop) {
    const line = sSample(loop);
    if (line) {
      const { start, end } = line;
      const t = sRand();
      return new Vector2(start.x + (end.x - start.x) * t, start.y + (end.y - start.y) * t);
    }
  }
  return void 0;
}

// src/class/planClass/raw/arrayVertical.ts
function handleVertical(result, parsedStyle, rayLoops) {
  const { diverse, vertical, elevations } = parsedStyle;
  rayLoops.forEach((rayLoop) => {
    rayLoop.forEach((ray) => {
      if (diverse) {
        elevations.forEach((elevation) => {
          vertical.forEach((params) => raySpacing(result, [elevation], ray, params));
        });
      } else {
        vertical.forEach((params) => {
          raySpacing(result, elevations, ray, params);
        });
      }
    });
  });
}
function raySpacing(result, elevations, ray, params) {
  const { ctrlArray, sandwich, endWidth, array } = params;
  const distance = ray.direction.length();
  const spaces = [];
  array.forEach((vu) => {
    for (let i = 0; i < vu.count; i++) spaces.push(vu.space);
  });
  const rc = matchRatioAndCount(spaces, distance, endWidth, sandwich);
  if (rc) {
    const arrayUnits = [];
    array.forEach((edgeUnit) => {
      const { space, replace } = edgeUnit;
      const tempBoxes = edgeUnitToTempBoxes(edgeUnit, rc.ratio);
      const replaceBoxes = replace ? edgeUnitToTempBoxes(replace, rc.ratio) : void 0;
      const spaceScaled = space * rc.ratio;
      for (let i = 0; i < edgeUnit.count; i++) {
        arrayUnits.push({
          spaceScaled,
          tempBoxes: replace && sRand() < replace.chance ? replaceBoxes : tempBoxes
        });
      }
    });
    const arrayDistance = arrayUnits.reduce((a, b) => a + b.spaceScaled, 0);
    const validIndexes = getValidIndexes(rc.count, ctrlArray);
    elevations.forEach((elevation) => {
      if (arrayUnits.length > 0) {
        const start = ray.start.clone();
        if (endWidth) {
          const d = ray.direction.clone();
          start.add(d.setLength(endWidth * rc.ratio));
        }
        const placeMatrix = new Matrix4().makeRotationZ(ray.direction.angle()).premultiply(TEMP.makeTranslation(start.x, start.y, elevation));
        validIndexes.forEach((i) => {
          let startD = i * arrayDistance;
          arrayUnits.forEach((tempArrayUnit, n) => {
            pushArraygDataToResult(tempArrayUnit, startD, placeMatrix, result);
            startD += (tempArrayUnit.spaceScaled + getLoopNext(arrayUnits, n).spaceScaled) / 2;
          });
        });
        if (sandwich)
          pushArraygDataToResult(arrayUnits[0], arrayDistance * rc.count, placeMatrix, result);
      }
    });
  }
}
function pushArraygDataToResult(tempArrayUnit, targetDistance, placeMatrix, result) {
  tempArrayUnit.tempBoxes?.forEach((tempBoxData) => {
    const mtx = tempBoxData.matrix.clone().premultiply(TEMP.makeTranslation(targetDistance, 0, 0)).premultiply(placeMatrix);
    pushBoxData(result, tempBoxData.color, mtx);
  });
}

// src/class/planClass/raw/arrayHorizontal.ts
function handleHorizontal(result, parsedStyle, rayLoops) {
  const { diverse, horizontal, elevations } = parsedStyle;
  if (diverse) {
    elevations.forEach((elevation) => {
      rayLoops.forEach((rayLoop) => {
        rayLoop.forEach((ray) => {
          horizontal.forEach((params) => {
            const tempBoxes = edgeFlexToTempBoxes(params, ray.direction.length());
            tempBoxes.forEach((tempBoxData) => {
              const matrix = tempBoxData.matrix.clone().premultiply(TEMP.makeRotationZ(ray.direction.angle())).premultiply(TEMP.makeTranslation(ray.start.x, ray.start.y, elevation));
              pushBoxData(result, tempBoxData.color, matrix);
            });
          });
        });
      });
    });
  } else {
    rayLoops.forEach((rayLoop) => {
      rayLoop.forEach((ray) => {
        horizontal.forEach((params) => {
          const tempBoxes = edgeFlexToTempBoxes(params, ray.direction.length());
          elevations.forEach((elevation) => {
            tempBoxes.forEach((tempBoxData) => {
              const matrix = tempBoxData.matrix.clone().premultiply(TEMP.makeRotationZ(ray.direction.angle())).premultiply(TEMP.makeTranslation(ray.start.x, ray.start.y, elevation));
              pushBoxData(result, tempBoxData.color, matrix);
            });
          });
        });
      });
    });
  }
}

// src/class/planClass/raw/planMatch.ts
function handleMatch(result, parsedStyle, rayLoops) {
  const { match, elevations } = parsedStyle;
  match.forEach((params) => {
    const { ctrlMatch, sandwich, array, along, simplify } = params;
    const { newRays, radian } = sRotateLinesAlong(rayLoops.flat(), along);
    const tempBoxRows = matchUnitsToTempBoxRows(array, newRays, sandwich, simplify);
    getValidIndexes(tempBoxRows.length, ctrlMatch).forEach((i) => {
      tempBoxRows[i].forEach((data) => {
        elevations.forEach((elevation) => {
          pushBoxData(
            result,
            data.color,
            data.matrix.clone().premultiply(TEMP.makeRotationZ(-radian)).premultiply(TEMP.makeTranslation(0, 0, elevation))
          );
        });
      });
    });
  });
}

// src/class/planClass/raw/planExtrude.ts
function handleExtrude(result, parsedStyle, rayLoops, globalMatchWidth) {
  const { extrude, elevations } = parsedStyle;
  extrude.forEach((params) => {
    const { color, trans, height, thickness } = params;
    const { index, glass } = sSample(color);
    const matrix = new Matrix4();
    if (thickness) {
      const moveY = thickness < 0 ? -0.5 : 0.5;
      const moveZ = height < 0 ? -0.5 : 0.5;
      const saveAs = result.instanced[glass ? "boxGlass" : "box"];
      rayLoops.forEach((rayLoop) => {
        rayLoop.forEach((ray) => {
          const boxMatrix = new Matrix4().premultiply(TEMP.makeTranslation(0.5, moveY, moveZ)).premultiply(
            TEMP.makeScale(ray.direction.length(), Math.abs(thickness), Math.abs(height))
          ).premultiply(TEMP.makeRotationZ(ray.direction.angle()));
          applyBasicTransform(params, boxMatrix);
          elevations.forEach((elevation) => {
            const m = boxMatrix.clone();
            m.premultiply(TEMP.makeTranslation(ray.start.x, ray.start.y, elevation));
            saveAs.matrices.push(m.toArray());
            saveAs.colors.push(index);
          });
        });
      });
    } else if (globalMatchWidth > 0) {
      const matchParam = {
        simplify: true,
        along: "WIDTH",
        ctrlMatch: void 0,
        sandwich: false,
        array: [
          {
            count: 1,
            unitDepth: globalMatchWidth,
            flexHeight: params.height,
            color,
            trans,
            shrink: void 0
          }
        ]
      };
      handleMatch(result, { match: [matchParam], elevations }, rayLoops);
    } else {
      const loop = outterRaysToLoop(rayLoops);
      const save = result.extruded[glass ? "glass" : "solid"];
      let saveFound = save.find((e) => isSameLoop(e.loop, loop));
      if (!saveFound) save.push(saveFound = { loop, matrices: [], colors: [] });
      const saveAs = saveFound;
      if (height < 0) matrix.premultiply(TEMP.makeTranslation(0, 0, -1));
      matrix.premultiply(TEMP.makeScale(1, 1, Math.abs(height)));
      applyBasicTransform(params, matrix);
      elevations.forEach((elevation) => {
        saveAs.matrices.push(
          matrix.clone().premultiply(TEMP.makeTranslation(0, 0, elevation)).toArray()
        );
        saveAs.colors.push(index);
      });
    }
  });
}
function isSameLoop(loopA, loopB) {
  return loopA.every((pt, i) => {
    const ptB = loopB[i];
    return ptB && pt[0] === ptB[0] && pt[1] === ptB[1];
  });
}
function outterRaysToLoop(rayLoops) {
  const result = [];
  const outter = rayLoops[0];
  if (outter) {
    const newLoopRays = getLoopWithoutIntersects(outter);
    newLoopRays.forEach((ray, i) => {
      result.push(ray.start.toArray());
      const nextRay = newLoopRays[i === newLoopRays.length - 1 ? 0 : i + 1];
      if (!ray.end.equals(nextRay.start)) {
        result.push(ray.end.toArray());
      }
    });
  }
  return result;
}
function getLoopWithoutIntersects(rayLoop) {
  let foundIntersects = true;
  while (foundIntersects) {
    let intersect;
    rayLoop.find((r, i) => {
      for (let j = i + 1; j < rayLoop.length; j++) {
        const nextRay = rayLoop[j];
        if (!nextRay.end.equals(r.start) && !nextRay.start.equals(r.end)) {
          const intersection = crossLines(r, nextRay);
          if (intersection) {
            intersect = [i, j, intersection];
            break;
          }
        }
      }
      if (intersect) return true;
    });
    if (intersect) {
      const [min, max, intersection] = intersect;
      const result = [];
      rayLoop.forEach((r, i) => {
        if (i < min || i > max) {
          result.push(r);
        } else if (i === min) {
          result.push({
            start: r.start,
            end: intersection.clone(),
            direction: intersection.clone().sub(r.start)
          });
        } else if (i === max) {
          result.push({
            start: intersection.clone(),
            end: r.end,
            direction: r.end.clone().sub(r.start)
          });
        }
      });
      rayLoop = result;
    } else {
      foundIntersects = false;
    }
  }
  return rayLoop;
}

// src/class/planClass/raw/handleRays.ts
function offsetRayLoops(rayLoops, sizeRef, params) {
  const offsetParams = params.asRatio ? { x: sizeRef.x * params.x, y: sizeRef.y * params.y } : { x: params.x, y: params.y };
  const offsetted = rayLoops.map((rayLoop) => rayLoop.map((ray) => offsetRay(ray, offsetParams)));
  return offsetted.map((rayLoop) => {
    const points = [];
    rayLoop.map((ray, i) => {
      const previous = rayLoop[i === 0 ? rayLoop.length - 1 : i - 1];
      const point = rayIntersectRay(ray, previous);
      if (point) points.push(point);
    });
    return points.map((start, j) => {
      const end = points[j === points.length - 1 ? 0 : j + 1];
      return { start, end, direction: end.clone().sub(start) };
    });
  });
}
function rectClampRayLoops(rayLoops, rectangles) {
  return rayLoops.map((rayLoop) => {
    const clamped = [];
    rectangles.forEach((rect) => {
      const { min, max } = rect;
      const pt1 = new Vector2(min.x, max.y);
      const pt2 = new Vector2(max.x, min.y);
      const clampRect = {
        min,
        max,
        lines: [
          { start: min, end: pt1 },
          { start: pt1, end: max },
          { start: max, end: pt2 },
          { start: pt2, end: min }
        ]
      };
      rayLoop.forEach((ray) => {
        const newLine = lineInsideRect(ray, clampRect);
        if (newLine) {
          const { start, end } = newLine;
          clamped.push({ start, end, direction: end.clone().sub(start) });
        }
      });
    });
    return clamped;
  });
}
function indentRayLoops(rayLoops, params) {
  const { central, asRatio, reverse, start, end } = params;
  return rayLoops.map((rayLoop) => {
    const newRayLoop = [];
    rayLoop.forEach((ray) => {
      const distance = ray.direction.length();
      const dStart = asRatio ? distance * start : start;
      const dEnd = asRatio ? distance * start : start;
      const mStart = central ? distance / 2 - dStart : dStart;
      const mEnd = central ? distance / 2 - dEnd : dEnd;
      if (reverse) {
        if (start) {
          const direction = ray.direction.clone().setLength(mStart);
          newRayLoop.push({
            start: ray.start.clone(),
            end: ray.start.clone().add(direction),
            direction
          });
        }
        if (end) {
          const direction = ray.direction.clone().setLength(mEnd);
          newRayLoop.push({
            start: ray.end.clone().add(direction.clone().negate()),
            end: ray.end.clone(),
            direction
          });
        }
      } else {
        const d = ray.direction.clone();
        const rayStart = ray.start.clone();
        const rayEnd = ray.end.clone();
        if (start) rayStart.add(d.setLength(mStart));
        if (end) rayEnd.add(d.setLength(-mEnd));
        newRayLoop.push({
          start: rayStart,
          end: rayEnd,
          direction: rayEnd.clone().sub(rayStart)
        });
      }
    });
    return newRayLoop;
  });
}
function splitRayloops(rayLoops, params) {
  const { array, sandwich, select } = params;
  function moveAndPushTo(saveAs, isSelected, point, move) {
    const start = point.clone();
    point.add(move);
    if (isSelected) saveAs.push({ start, end: point.clone(), direction: move.clone() });
  }
  return rayLoops.map((rayLoop) => {
    const newRayLoop = [];
    rayLoop.forEach((ray) => {
      const distance = ray.direction.length();
      const rc = matchRatioAndCount(array, distance, array[0], sandwich);
      if (rc) {
        const pt = ray.start.clone();
        const moves = array.map((s) => ray.direction.clone().setLength(s * rc.ratio));
        for (let i = 0; i < rc.count; i++) {
          moves.forEach((m, n) => moveAndPushTo(newRayLoop, n === select, pt, m));
        }
        if (sandwich && select === 0) moveAndPushTo(newRayLoop, true, pt, moves[0]);
      }
    });
    return newRayLoop;
  });
}

// src/class/planClass/model/generate.ts
import {
  Matrix4 as Matrix42,
  Group,
  Color,
  Vector2 as Vector24,
  Shape,
  ExtrudeGeometry,
  EdgesGeometry,
  LineSegments,
  BufferGeometry,
  BoxGeometry,
  InstancedMesh,
  InstancedBufferGeometry,
  InstancedBufferAttribute,
  BufferAttribute
} from "three";

// src/class/planClass/model/materials.ts
import {
  MeshBasicMaterial,
  MeshLambertMaterial,
  MeshStandardMaterial,
  LineBasicMaterial
} from "three";

// src/color.ts
var COLOR = {
  GLASS: "#8bf G",
  CONCRETE: "#eee",
  METAL: "#666",
  WOOD: "#eb7",
  BRICK: "#d66",
  ROOF: "#333"
};
var _COLOR = {
  GROUND: "#eee",
  EDGE: "#000",
  SKY: "#fff"
};

// src/class/planClass/model/materials.ts
var basicFaceMaterials = {
  solid: new MeshBasicMaterial(),
  glass: new MeshBasicMaterial(),
  roof: new MeshBasicMaterial({ side: 2 })
};
var presetFaceMaterials = {
  solid: new MeshLambertMaterial(),
  glass: new MeshStandardMaterial({
    side: 2,
    opacity: 0.6,
    transparent: true
  }),
  roof: new MeshLambertMaterial({ side: 2 })
};
var presetOtherMaterials = {
  edge: new LineBasicMaterial({ color: _COLOR.EDGE }),
  ground: new MeshLambertMaterial({
    color: _COLOR.GROUND,
    polygonOffset: true,
    polygonOffsetFactor: 1,
    polygonOffsetUnits: 0.1
  })
};
presetOtherMaterials.edge.onBeforeCompile = (shader) => {
  shader.vertexShader = shader.vertexShader.replace("void main() {", `attribute mat4 matrix; void main() {`).replace(
    "#include <project_vertex>",
    `vec4 mvPosition = matrix * vec4( transformed, 1.0 ); gl_Position = projectionMatrix * modelViewMatrix * mvPosition;`
  );
};

// src/class/planClass/model/generate.ts
var boxGeom = new BoxGeometry();
var slopeGeom = {};
function getSlopeGeom(type) {
  let g = slopeGeom[type];
  if (!g) {
    g = type === "2" ? getSlope2() : getSlope4();
    slopeGeom[type] = g;
  }
  return g;
}
function generate(raw, params) {
  const tempResult = {
    instanced: {
      box: { color: [], matrix: [] },
      boxGlass: { color: [], matrix: [] },
      slope2: { color: [], matrix: [] },
      slope2Glass: { color: [], matrix: [] },
      slope4: { color: [], matrix: [] },
      slope4Glass: { color: [], matrix: [] }
    },
    instancedEdge: { boxAttribute: [], slope2Attribute: [], slope4Attribute: [] },
    extruded: { solid: [], glass: [] }
  };
  const model = new Group().rotateX(-Math.PI / 2);
  const rawToInstancedParams = {
    tempMatrix: new Matrix42(),
    finalColors: getRemappedColors(raw.colors, params?.remap),
    greyScale: params?.greyScale || false
  };
  const tr = params?.inplace ? { center: raw.centerRelative, rotate: raw.rotate } : void 0;
  rawToInstancedTemp(raw.instanced, tempResult, tr, rawToInstancedParams);
  rawToExtrudedTemp(raw.extruded, tempResult, tr, rawToInstancedParams);
  function addInstanceByKey(key, geom, mat) {
    addInstance(key, tempResult.instanced[key], model, geom, mat);
  }
  const useMaterials = params?.basicMaterial ? basicFaceMaterials : presetFaceMaterials;
  addInstanceByKey("box", boxGeom, useMaterials.solid);
  addInstanceByKey("boxGlass", boxGeom, useMaterials.glass);
  addInstanceByKey("slope2", getSlopeGeom("2"), useMaterials.roof);
  addInstanceByKey("slope4", getSlopeGeom("4"), useMaterials.roof);
  addInstanceByKey("slope2Glass", getSlopeGeom("2"), useMaterials.glass);
  addInstanceByKey("slope4Glass", getSlopeGeom("4"), useMaterials.glass);
  tempResult.extruded.solid.forEach((data) => {
    addInstance("extrudedSolid", data, model, data.geom, useMaterials.solid);
  });
  tempResult.extruded.glass.forEach((data) => {
    addInstance("extrudedGlass", data, model, data.geom, useMaterials.glass);
  });
  if (params?.edge) {
    const edgeMat = presetOtherMaterials.edge;
    addInstancedEdges(tempResult.instancedEdge.boxAttribute, model, boxGeom, edgeMat);
    addInstancedEdges(tempResult.instancedEdge.slope2Attribute, model, getSlopeGeom("2"), edgeMat);
    addInstancedEdges(tempResult.instancedEdge.slope4Attribute, model, getSlopeGeom("4"), edgeMat);
    tempResult.extruded.solid.forEach((data) => {
      addInstancedEdges(data.edgeAttr, model, data.geom, edgeMat);
    });
    tempResult.extruded.glass.forEach((data) => {
      addInstancedEdges(data.edgeAttr, model, data.geom, edgeMat);
    });
  }
  model.userData.magizType = "building";
  return model;
}
function getInstanceMatrix(m, transRelative, tempMatrix) {
  const matrix = new Matrix42().fromArray(m);
  if (transRelative) {
    matrix.premultiply(tempMatrix.makeRotationZ(transRelative.rotate)).premultiply(tempMatrix.makeTranslation(...transRelative.center, 0));
  }
  return matrix;
}
function rawToInstancedTemp(raw, result, transRelative, params) {
  let instanceType;
  const { tempMatrix, finalColors, greyScale } = params;
  for (instanceType in raw) {
    const instancedData = raw[instanceType];
    const resultRawData = result.instanced[instanceType];
    instancedData.matrices.forEach((m, i) => {
      const matrix = getInstanceMatrix(m, transRelative, tempMatrix);
      result.instancedEdge[instanceType.includes("box") ? "boxAttribute" : instanceType.includes("slope2") ? "slope2Attribute" : "slope4Attribute"].push(...matrix.toArray());
      resultRawData.matrix.push(matrix);
      if (!greyScale) {
        const c = finalColors[instancedData.colors[i] || 0];
        if (c) resultRawData.color.push(new Color(c));
      }
    });
  }
}
var extrudeParams = { depth: 1, bevelEnabled: false };
function rawToExtrudedTemp(raw, result, restoreParams, params) {
  const { tempMatrix, finalColors, greyScale } = params;
  let key;
  for (key in raw) {
    raw[key].forEach((extrudedInstancedData) => {
      const shape = new Shape(extrudedInstancedData.loop.map((pt) => new Vector24(...pt)));
      const geom = new ExtrudeGeometry(shape, extrudeParams);
      const resultRawData = { geom, edgeAttr: [], matrix: [], color: [] };
      extrudedInstancedData.matrices.forEach((matrixArray, i) => {
        const matrix = getInstanceMatrix(matrixArray, restoreParams, tempMatrix);
        resultRawData.edgeAttr.push(...matrix.toArray());
        resultRawData.matrix.push(matrix);
        if (!greyScale) {
          const c = finalColors[extrudedInstancedData.colors[i] || 0];
          if (c) resultRawData.color.push(new Color(c));
        }
      });
      result.extruded[key].push(resultRawData);
    });
  }
}
function getRemappedColors(colors, remap) {
  const finalRemap = [];
  if (remap) {
    let k;
    if (remap.face) {
      for (k in remap.face) {
        const c = remap.face[k];
        if (c) finalRemap.push({ from: COLOR[k], to: c });
      }
    }
    if (remap.custom) remap.custom.forEach((r) => finalRemap.push(r));
  }
  return colors.map((x) => finalRemap.find((r) => r.from === x)?.to || x);
}
function addInstancedEdges(matrixAttribute, building, geom, edgeShaderMaterial) {
  if (matrixAttribute.length > 0) {
    const eg = new EdgesGeometry(geom);
    const eibg = new InstancedBufferGeometry();
    const ls = new LineSegments(eibg, edgeShaderMaterial);
    eibg.setAttribute("position", eg.getAttribute("position"));
    eibg.setAttribute(
      "matrix",
      new InstancedBufferAttribute(new Float32Array(matrixAttribute), 16)
    );
    eibg.instanceCount = matrixAttribute.length / 16;
    eg.dispose();
    ls.frustumCulled = false;
    ls.renderOrder = 9;
    building.add(ls);
  }
}
function addInstance(type, data, group, geom, mat) {
  if (data.matrix.length > 0) {
    const iMesh = new InstancedMesh(geom, mat, data.matrix.length);
    data.matrix.forEach((m, n) => {
      iMesh.setMatrixAt(n, m);
      const c = data.color[n];
      if (c) iMesh.setColorAt(n, c);
    });
    iMesh.castShadow = iMesh.receiveShadow = true;
    iMesh.userData.magizType = type;
    group.add(iMesh);
  }
}
function getSlope4(indentRatio = 0.2) {
  const geometry = new BufferGeometry();
  const t1 = [indentRatio, 0.5, 1];
  const t2 = [1 - indentRatio, 0.5, 1];
  const r1 = [0, 0, 0];
  const r2 = [1, 0, 0];
  const r3 = [1, 1, 0];
  const r4 = [0, 1, 0];
  const vertices = new Float32Array([
    ...r2,
    ...t1,
    ...r1,
    ...r2,
    ...t2,
    ...t1,
    ...r4,
    ...t2,
    ...r3,
    ...r4,
    ...t1,
    ...t2,
    ...r1,
    ...t1,
    ...r4,
    ...r3,
    ...t2,
    ...r2
  ]);
  return geometry.setAttribute("position", new BufferAttribute(vertices, 3));
}
function getSlope2() {
  const geometry = new BufferGeometry();
  const t1 = [0, 0.5, 1];
  const t2 = [1, 0.5, 1];
  const r1 = [0, 0, 0];
  const r2 = [1, 0, 0];
  const r3 = [1, 1, 0];
  const r4 = [0, 1, 0];
  const vertices = new Float32Array([
    ...r2,
    ...t1,
    ...r1,
    ...r2,
    ...t2,
    ...t1,
    ...r4,
    ...t2,
    ...r3,
    ...r4,
    ...t1,
    ...t2
  ]);
  return geometry.setAttribute("position", new BufferAttribute(vertices, 3));
}

// src/class/planClass/index.ts
var Plan = class {
  /** 建筑模型生成的参数 */
  styleParams;
  /** 平面的面积 */
  area;
  /** 平面的中心点坐标 （用于移动模型到原位） */
  center;
  /** 平面的中心点对齐原点并将长边对齐轴线后的相对平面（用于简化计算） */
  relative;
  /** 输入的源坐标 */
  orignal;
  /** 创建建筑平面实例 */
  constructor(input) {
    this.orignal = input.loops.map((loop) => loop.map((pt) => new Vector2(...pt)));
    const outterLoop = this.orignal[0];
    const bounds = getBounds(outterLoop);
    if (outterLoop && bounds) {
      if (ShapeUtils.isClockWise(outterLoop)) outterLoop.reverse();
      this.area = ShapeUtils.area(outterLoop);
      for (let i = 1; i < this.orignal.length; i++) {
        const loop = this.orignal[i];
        if (!ShapeUtils.isClockWise(loop)) loop.reverse();
        this.area -= ShapeUtils.area(loop);
      }
      const longest = new Vector2();
      outterLoop.forEach((current, i) => {
        const next = outterLoop[i + 1 === outterLoop.length ? 0 : i + 1];
        const v2 = new Vector2().subVectors(next, current);
        if (v2.length() > longest.length()) longest.copy(v2);
      });
      const origin = new Vector2(0, 0);
      const radian = longest.angle();
      this.center = getCenter(outterLoop);
      const relativePoints = this.orignal.map(
        (loop) => loop.map((v2) => v2.sub(this.center).rotateAround(origin, -radian))
      );
      this.relative = {
        radian,
        bounds,
        size: { x: bounds.max.x - bounds.min.x, y: bounds.max.y - bounds.min.y },
        rayLoops: relativePoints.map(
          (loop) => loop.map((start, i) => {
            const end = loop[i + 1 === loop.length ? 0 : i + 1];
            const direction = end.clone().sub(start);
            return { start, end, direction };
          })
        )
      };
    } else {
      throw "ERROR: invalid input.loops";
    }
    this.styleParams = Object.assign(
      {
        style: "",
        height: 24,
        floorHeight: 3,
        elevation: 0,
        seed: 0,
        match: 2
      },
      input.params
    );
  }
  /** 根据样式参数中的 setEdges 处理边线向量并生成新的向量数组。不处理内部的边线。 */
  getEdges(edgeParams) {
    let rayLoops = [];
    const outterLoop = this.relative.rayLoops[0];
    const bounds = getBounds(outterLoop?.map((line) => line.start));
    if (outterLoop && bounds) {
      const sizeRef = { x: 0, y: 0, caculated: false };
      rayLoops.push(outterLoop);
      edgeParams.forEach((params) => {
        const { offset, along, clamp, indent, split } = params;
        if (offset) {
          if (!sizeRef.caculated) {
            const { min, max } = bounds;
            sizeRef.x = max.x - min.x;
            sizeRef.y = max.y - min.y;
            sizeRef.caculated = true;
          }
          rayLoops = offsetRayLoops(rayLoops, sizeRef, offset);
        } else if (clamp) {
          const rects = getClampedRects(bounds, clamp);
          rayLoops = rectClampRayLoops(rayLoops, rects);
        } else if (along !== void 0) {
          switch (along) {
            case "WIDTH":
              rayLoops = rayLoops.map((loop) => loop.filter((ray) => isAlongAxis(ray.direction.angle()))).filter((loop) => loop.length > 0);
              break;
            case "DEPTH":
              rayLoops = rayLoops.map((loop) => loop.filter((ray) => !isAlongAxis(ray.direction.angle()))).filter((loop) => loop.length > 0);
              break;
            case "RANDOM":
              rayLoops = rayLoops.map((loop) => loop.filter(() => sRand() < 1)).filter((loop) => loop.length > 0);
              break;
            case "SHORTEST":
            case "LONGEST":
              {
                const { direction } = outterLoop.reduce(
                  (a, b) => a.direction.length() > b.direction.length() ? a : b
                );
                rayLoops = along === "LONGEST" ? rayLoops.map(
                  (loop) => loop.filter((ray) => isAlongAxis(ray.direction.angleTo(direction)))
                ) : rayLoops.map(
                  (loop) => loop.filter((ray) => !isAlongAxis(ray.direction.angleTo(direction)))
                );
              }
              break;
            default:
              rayLoops = rayLoops.map(
                (loop) => loop.filter(
                  (ray) => Math.abs(ray.direction.angle() - along) < 10 * (Math.PI / 180)
                )
              ).filter((loop) => loop.length > 0);
              break;
          }
        } else if (indent) {
          rayLoops = indentRayLoops(rayLoops, indent);
        } else if (split) {
          rayLoops = splitRayloops(rayLoops, split);
        }
      });
    }
    return rayLoops;
  }
  /** 按平面和样式生成可序列化的建筑模型数据 */
  toRaw(styles3, origin) {
    const centerRelative = [
      this.center.x - (origin?.x || 0),
      this.center.y - (origin?.y || 0)
    ];
    const { colors, floorCount, classified } = styles3.parseStyle(this.styleParams);
    const raw = {
      info: { floorArea: this.area, floors: floorCount },
      points: this.relative.rayLoops.map((loop) => loop.map((ray) => ray.start.toArray())),
      center: this.center.toArray(),
      centerRelative,
      rotate: this.relative.radian,
      params: this.styleParams,
      instanced: {
        box: { matrices: [], colors: [] },
        boxGlass: { matrices: [], colors: [] },
        slope2: { matrices: [], colors: [] },
        slope2Glass: { matrices: [], colors: [] },
        slope4: { matrices: [], colors: [] },
        slope4Glass: { matrices: [], colors: [] }
      },
      extruded: { solid: [], glass: [] },
      colors
    };
    for (const edgeParamsJSON in classified) {
      const { params, parsed } = classified[edgeParamsJSON];
      const rayLoops = this.getEdges(params);
      parsed.forEach((dataParsed) => {
        handleExtrude(raw, dataParsed, rayLoops, this.styleParams.match);
        handleMatch(raw, dataParsed, rayLoops);
        handleVertical(raw, dataParsed, rayLoops);
        handleHorizontal(raw, dataParsed, rayLoops);
        handleAppendent(raw, dataParsed, rayLoops);
        const outterLoop = rayLoops[0];
        const bounds = getBounds(outterLoop?.map((r) => r.start));
        if (outterLoop && bounds) {
          handleBoundingBox(raw, dataParsed, bounds);
          handleSlopingRoof(raw, dataParsed, bounds);
        }
      });
    }
    return raw;
  }
  toModel(styles3, generateParams, origin) {
    const raw = this.toRaw(styles3, origin);
    return generate(raw, generateParams);
  }
};
function getCenter(loop) {
  const c = [0, 0];
  loop.forEach((v2) => {
    c[0] += v2.x;
    c[1] += v2.y;
  });
  c[0] /= loop.length;
  c[1] /= loop.length;
  return new Vector2(...c);
}

// src/class/styleClass/handleParse.ts
var import_math_expression_evaluator = __toESM(require_es());
var evaluator = new import_math_expression_evaluator.default();
var MAIN = { UNITS: {} };
var PRESET = {
  UNITS: void 0,
  COLOR: void 0
};
var RESULT = {
  colors: [],
  floorCount: 0,
  classified: {}
};
function parseBool(b) {
  if (typeof b === "boolean") {
    return b ? 1 : 0;
  } else {
    return parse(b) ? 1 : 0;
  }
}
function parse(ns, defaultValue = 0) {
  let n;
  if (typeof ns === "string") {
    ns = replaceUnit(ns, MAIN.UNITS);
    ns = replaceUnit(ns, PRESET.UNITS);
    try {
      n = evaluator.eval(ns);
    } catch (error) {
      console.error("ns parsing:", ns, error);
      n = defaultValue;
    }
  } else {
    n = ns || defaultValue;
  }
  return n;
}
function replaceUnit(input, units) {
  if (units) {
    const keys = Object.keys(units).sort((a, b) => b.length - a.length);
    keys.forEach((k) => {
      input = input.replace(new RegExp(`(\\d+(\\.\\d+)?)?${k}`, "g"), (m) => {
        const u = Number(units[k]);
        const n = Number(m.replace(k, "")) || 1;
        return (n * u).toString();
      });
    });
  }
  return input;
}
function parseIndent(params) {
  return params ? {
    start: parse(params.start),
    end: parse(params.end),
    central: parseBool(params.central),
    asRatio: parseBool(params.asRatio),
    reverse: parseBool(params.reverse)
  } : void 0;
}
function formatColor(colorMap, color) {
  const a = Array.isArray(color) ? color : [color];
  return a.map((x) => {
    let c = x ? x.trim() : COLOR.CONCRETE;
    if (c === "G") c = COLOR.GLASS;
    const cv = c.replace(/ *G$/, "");
    let index = colorMap.indexOf(cv);
    if (index < 0) {
      index = colorMap.length;
      colorMap.push(cv);
    }
    return { index, glass: /G$/.test(c) ? 1 : 0 };
  });
}
function parseStatus(status, data) {
  let c = status.color;
  if (typeof c === "string") c = PRESET.COLOR?.[c] || c;
  return Object.assign(
    {
      trans: status.trans ? status.trans.map((transform) => {
        if ("rotateX" in transform) {
          return { rotateX: parse(transform.rotateX) };
        } else if ("rotateY" in transform) {
          return { rotateY: parse(transform.rotateY) };
        } else if ("rotateZ" in transform) {
          return { rotateZ: parse(transform.rotateZ) };
        } else {
          return {
            moveX: parse(transform.moveX),
            moveY: parse(transform.moveY),
            moveZ: parse(transform.moveZ)
          };
        }
      }) : [],
      color: formatColor(RESULT.colors, c)
    },
    data
  );
}
function parseBoxes(params) {
  return params ? params.map(
    (b) => parseStatus(b, {
      widthX: parse(b.widthX),
      depthY: parse(b.depthY),
      heightZ: parse(b.heightZ)
    })
  ) : [];
}
function parseFlexes(params) {
  return params ? params.map((x) => {
    return parseStatus(x, {
      unitHeight: parse(x.unitHeight),
      flexHeight: parse(x.flexHeight),
      flexDepth: parse(x.flexDepth),
      flexWidth: parse(x.flexWidth),
      dash: parseControl(x.dash),
      shrink: parseIndent(x.shrink),
      seg: parseBool(x.seg)
    });
  }) : [];
}
function parseControl(params) {
  return params ? {
    total: parse(params.total),
    indent: parseIndent(params.indent),
    filter: params.filter?.map(parse),
    chance: parse(params.chance)
  } : void 0;
}

// src/class/styleClass/index.ts
var MagizStyles = class {
  /** 默认样式 Blocks */
  Blocks;
  /** 整合后的样式参数 */
  data;
  /** 创建样式库实例，输入的样式将自动整合 */
  constructor(stylesArray) {
    this.Blocks = {
      type: "FREE",
      tags: {},
      section: {
        bottom: {
          height: "1H",
          floor: [{ ctrlFloor: { total: 1 }, extrude: [{ height: "1H" }] }]
        }
      }
    };
    this.data = {};
    this.merge(stylesArray);
  }
  merge(stylesArray) {
    stylesArray.forEach((style) => {
      for (const key in style) {
        if (this.data[key]) console.warn(`Style overwrited: ${key}`);
        this.data[key] = style[key];
      }
    });
  }
  /** 输入注册状态，检查样式是否可用 */
  isValid(name, regState) {
    if (name === "Blocks") {
      return true;
    } else {
      const found = this.data[name];
      return found && (found.type === "FREE" || regState) ? true : false;
    }
  }
  getNames() {
    return Object.keys(this.data);
  }
  /** 根据输入参数和随机种子解析样式。优先按custom解析 */
  parseStyle(styleParams) {
    RESULT.colors = [];
    RESULT.floorCount = 0;
    RESULT.classified = {};
    const height = Number(styleParams.height);
    const floorHeight = Number(styleParams.floorHeight) || 3;
    const elevation = Number(styleParams.elevation);
    const styleSelected = !styleParams.style || styleParams.style === "Blocks" ? this.Blocks : this.data[styleParams.style];
    if (styleSelected) {
      MAIN.UNITS = Object.assign({ H: height, FH: floorHeight }, styleSelected.unit);
      const ss = styleSelected.section;
      const rsh = parse(ss.roof?.height);
      const rfh = parse(ss.roof?.floorHeight, floorHeight);
      let bsh = parse(ss.bottom.height);
      let bfh = parse(ss.bottom.floorHeight, floorHeight);
      if (bsh < bfh) bsh = bfh;
      let msh = height - rsh - bsh;
      const mfh = parse(ss.middle?.floorHeight, floorHeight);
      const middleFloors = Math.floor(msh / mfh);
      msh = middleFloors * mfh;
      bsh = height - rsh - msh;
      bfh = bsh / Math.floor(bsh / bfh);
      MAIN.UNITS.RH = rsh;
      MAIN.UNITS.MH = msh;
      MAIN.UNITS.BH = bsh;
      parseSection("bottom", ss, elevation, bsh, bfh);
      parseSection("middle", ss, elevation + bsh, msh, mfh);
      parseSection("roof", ss, elevation + height - rsh, rsh, rfh);
    } else {
      console.warn(`${styleParams.style} is invalid, ignored.`);
    }
    return RESULT;
  }
};
function parseSection(type, sectionParams, sectionElevation, sectionHeight, floorHeight) {
  const section = sectionParams[type];
  if (section) {
    MAIN.UNITS.SH = sectionHeight;
    MAIN.UNITS.FH = floorHeight;
    let floorCount = 0;
    if (type === "roof") {
      floorCount = 1;
    } else {
      floorCount = Math.round(sectionHeight / floorHeight);
      RESULT.floorCount += floorCount;
    }
    if (floorCount > 0) {
      section.floor?.forEach((floorParams) => {
        parseFloor(floorCount, floorHeight, sectionElevation, floorParams);
        floorParams.presets?.forEach((floorPresetParams) => {
          const { unit, color, floor } = floorPresetParams;
          PRESET.UNITS = {};
          PRESET.COLOR = {};
          for (const key in unit) {
            PRESET.UNITS[key] = parse(unit[key]);
          }
          for (const key in color) {
            PRESET.COLOR[key] = color[key];
          }
          floor.forEach((params) => {
            const presetClone = { ...params };
            if (floorParams.ctrlFloor) presetClone.ctrlFloor = floorParams.ctrlFloor;
            presetClone.edge = [...floorParams.edge || [], ...presetClone.edge || []];
            parseFloor(floorCount, floorHeight, sectionElevation, presetClone);
          });
          PRESET.UNITS = void 0;
          PRESET.COLOR = void 0;
        });
      });
    }
  }
}
function parseFloor(totalFloors, floorHeight, sectionElevation, floorParams) {
  const elevations = getValidIndexes(totalFloors, parseControl(floorParams.ctrlFloor)).map(
    (i) => sectionElevation + i * floorHeight
  );
  const edgeParams = [];
  floorParams.edge?.forEach((handleEdge) => {
    const { offset, along, clamp, indent, split } = handleEdge;
    if (offset) {
      edgeParams.push({ offset: parseOffsetEdge(offset) });
    } else if (along) {
      edgeParams.push({ along });
    } else if (clamp) {
      edgeParams.push({ clamp: parseClamp(clamp) });
    } else if (indent) {
      edgeParams.push({ indent: parseIndent(indent) });
    } else if (split) {
      edgeParams.push({ split: parseSplit(split) });
    }
  });
  const edgeParamsJSON = JSON.stringify(edgeParams);
  const saveAs = {
    diverse: parseBool(floorParams.diverse),
    elevations,
    edgeParams,
    extrude: [],
    match: [],
    vertical: [],
    horizontal: [],
    appendent: [],
    boundingBox: [],
    slopingRoof: []
  };
  const sameEdge = RESULT.classified[edgeParamsJSON];
  sameEdge ? sameEdge.parsed.push(saveAs) : RESULT.classified[edgeParamsJSON] = { params: edgeParams, parsed: [saveAs] };
  parseExtrude(saveAs, floorParams);
  parseMatch(saveAs, floorParams);
  parseVertical(saveAs, floorParams);
  parseHorizontal(saveAs, floorParams);
  parseAppendent(saveAs, floorParams);
  parseSlopingRoof(saveAs, floorParams);
  parseBoundingBox(saveAs, floorParams);
}
function parseOffsetEdge(params) {
  if (typeof params === "object") {
    return {
      x: parse(params.x),
      y: parse(params.y),
      asRatio: parseBool(params.asRatio)
    };
  } else {
    const x = parse(params);
    return { x, y: x, asRatio: 0 };
  }
}
function parseExtrude(to, params) {
  params?.extrude?.forEach((p) => {
    to.extrude.push(
      parseStatus(p, {
        height: parse(p.height),
        thickness: parse(p.thickness)
      })
    );
  });
}
function parseMatch(to, params) {
  params?.match?.forEach((p) => {
    to.match.push({
      array: p.array.map((u) => {
        return parseStatus(u, {
          count: parse(u.count, 1),
          unitDepth: parse(u.unitDepth),
          flexHeight: parse(u.flexHeight),
          shrink: parseIndent(u.shrink)
        });
      }),
      along: p.along || "WIDTH",
      ctrlMatch: parseControl(p.ctrlMatch),
      sandwich: parseBool(p.sandwich),
      simplify: parseBool(p.simplify)
    });
  });
}
function parseVertical(to, params) {
  params?.vertical?.forEach((p) => {
    to.vertical.push({
      array: p.array.map((unit) => {
        const { replace } = unit;
        return {
          space: parse(unit.space),
          boxes: parseBoxes(unit.boxes),
          flexes: parseFlexes(unit.flexes),
          replace: replace ? {
            chance: parse(replace.chance),
            boxes: parseBoxes(replace.boxes),
            flexes: parseFlexes(replace.flexes)
          } : void 0,
          count: parse(unit.count, 1)
        };
      }),
      ctrlArray: parseControl(p.ctrlArray),
      sandwich: parseBool(p.sandwich),
      endWidth: parse(p.endWidth)
    });
  });
}
function parseHorizontal(to, params) {
  params?.horizontal?.forEach((p) => {
    to.horizontal.push(
      parseStatus(p, {
        array: p.array.map(parse),
        flexDepth: parse(p.flexDepth),
        flexHeight: parse(p.flexHeight),
        seg: parseBool(p.seg),
        sandwich: parseBool(p.sandwich),
        ctrlFlex: parseControl(p.ctrlFlex),
        shrink: parseIndent(p.shrink)
      })
    );
  });
}
function parseSlopingRoof(to, params) {
  params?.slopingRoof?.forEach((p) => {
    to.slopingRoof.push(
      parseStatus(p, {
        form: p.form,
        overhang: parse(p.overhang),
        height: parse(p.height)
      })
    );
  });
}
function parseBoundingBox(to, params) {
  params?.boundingBox?.forEach((p) => {
    to.boundingBox.push(
      parseStatus(p, {
        height: parse(p.height),
        clamp: parseClamp(p.clamp)
      })
    );
  });
}
function parseAppendent(to, params) {
  params?.appendent?.forEach((p) => {
    to.appendent.push({
      count: parse(p.count, 1),
      place: p.place || "EDGE",
      boxes: parseBoxes(p.boxes)
    });
  });
}
function parseClamp(params) {
  return params ? {
    startX: parse(params.startX),
    startY: parse(params.startY),
    centralX: parseBool(params.centralX),
    endX: parse(params.endX),
    endY: parse(params.endY),
    centralY: parseBool(params.centralY),
    asRatio: parseBool(params.asRatio),
    reverse: parseBool(params.reverse)
  } : void 0;
}
function parseSplit(params) {
  return {
    array: params.array.map(parse),
    select: params.select,
    sandwich: parseBool(params.sandwich)
  };
}

// src/class/styleClass/utils.ts
function check(params) {
  return params;
}
function preset(floorPreset, params) {
  return {
    floor: floorPreset.floor,
    unit: Object.assign({ ...floorPreset.unit }, params?.unit),
    color: Object.assign({ ...floorPreset.color }, params?.color)
  };
}

// src/styles/preset.ts
var simple = {
  "T:\u5973\u513F\u5899": check({
    unit: { \u7F29\u8FDB: 0, \u9AD8\u5EA6: 1.2, \u539A\u5EA6: 0.2, \u62AC\u5347: 0 },
    color: { \u989C\u8272: "#eee" },
    floor: [
      {
        edge: [{ offset: "\u7F29\u8FDB" }],
        extrude: [
          {
            height: "\u9AD8\u5EA6",
            thickness: "\u539A\u5EA6",
            color: "\u989C\u8272",
            trans: [{ moveZ: "\u62AC\u5347" }]
          }
        ]
      }
    ]
  }),
  "M:\u6A2A\u5411": check({
    unit: { \u697C\u677F\u51FA\u6311: 1, \u697C\u677F\u539A: 1, \u5E95\u90E8\u4FEE\u6B63: 0 },
    color: { \u989C\u8272: COLOR.METAL },
    floor: [
      {
        ctrlFloor: { indent: { start: "\u5E95\u90E8\u4FEE\u6B63", end: -1 } },
        edge: [{ offset: "-\u697C\u677F\u51FA\u6311" }],
        extrude: [{ height: "\u697C\u677F\u539A", trans: [{ moveZ: "-0.5\u697C\u677F\u539A" }] }]
      },
      {
        ctrlFloor: { total: 1 },
        extrude: [{ height: "SH", color: "G" }]
      }
    ]
  }),
  "M:\u7AD6\u5411": check({
    unit: { \u95F4\u8DDD: 8, \u67F1\u5BBD: 2, \u67F1\u6DF1: 2, \u697C\u677F\u7F29\u8FDB: 0.5, \u5E95\u90E8\u4FEE\u6B63: 0 },
    color: { \u5BBD\u67F1\u989C\u8272: COLOR.CONCRETE, \u67F1\u989C\u8272: COLOR.METAL },
    floor: [
      {
        ctrlFloor: { indent: { start: "\u5E95\u90E8\u4FEE\u6B63", end: -1 } },
        edge: [{ offset: "\u697C\u677F\u7F29\u8FDB" }],
        extrude: [{ height: 0.6, trans: [{ moveZ: -0.3 }] }]
      },
      {
        ctrlFloor: { total: 1 },
        extrude: [{ height: "SH", color: "G" }],
        vertical: [
          {
            array: [
              {
                space: "\u95F4\u8DDD",
                boxes: [{ widthX: "\u67F1\u5BBD", depthY: "\u67F1\u6DF1", heightZ: "SH", color: "\u5BBD\u67F1\u989C\u8272" }]
              }
            ]
          }
        ]
      }
    ]
  }),
  /** 不会像 “M:竖向” 因柱深过大而出挑 */
  "M:\u7AD6\u5411\u8FDB\u6DF1\u5BF9\u9F50": check({
    unit: {
      \u95F4\u8DDD: 6,
      \u67F1\u5BBD: 4,
      \u67F1\u6DF1: 1,
      \u697C\u677F\u7F29\u8FDB: 0.5,
      \u5E95\u90E8\u4FEE\u6B63: 0,
      /** 尽端无出挑：0.5柱宽-0.5柱深 */
      \u8FDB\u6DF1\u5BF9\u9F50: 0,
      \u4EA4\u53C9: 1
    },
    color: { \u5BBD\u67F1\u989C\u8272: COLOR.CONCRETE, \u67F1\u989C\u8272: COLOR.METAL },
    floor: [
      {
        ctrlFloor: { indent: { start: "\u5E95\u90E8\u4FEE\u6B63", end: -1 } },
        edge: [{ offset: "\u697C\u677F\u7F29\u8FDB" }],
        extrude: [{ height: 0.6, trans: [{ moveZ: -0.3 }] }]
      },
      {
        ctrlFloor: { total: 1 },
        extrude: [{ height: "SH", color: "G" }],
        vertical: [
          {
            sandwich: "\u4EA4\u53C9",
            endWidth: "\u8FDB\u6DF1\u5BF9\u9F50",
            array: [
              {
                space: "\u95F4\u8DDD",
                boxes: [{ widthX: "\u67F1\u5BBD", depthY: "\u67F1\u6DF1", heightZ: "SH", color: "\u5BBD\u67F1\u989C\u8272" }]
              }
            ]
          }
        ]
      }
    ]
  }),
  "M:\u7AD6\u5411\u5C3D\u7AEF\u4EA4\u53C9": check({
    unit: { \u95F4\u8DDD: 6, \u67F1\u5BBD: 4, \u67F1\u6DF1: 1, \u697C\u677F\u7F29\u8FDB: 0.5, \u5E95\u90E8\u4FEE\u6B63: 0 },
    color: { \u5BBD\u67F1\u989C\u8272: COLOR.CONCRETE, \u67F1\u989C\u8272: COLOR.METAL },
    floor: [
      {
        ctrlFloor: { indent: { start: "\u5E95\u90E8\u4FEE\u6B63", end: -1 } },
        edge: [{ offset: "\u697C\u677F\u7F29\u8FDB" }],
        extrude: [{ height: 0.6, trans: [{ moveZ: -0.3 }] }]
      },
      {
        ctrlFloor: { total: 1 },
        extrude: [{ height: "SH", color: "G" }],
        vertical: [
          {
            sandwich: true,
            endWidth: 1,
            array: [
              {
                space: "\u95F4\u8DDD",
                boxes: [{ widthX: "\u67F1\u5BBD", depthY: "\u67F1\u6DF1", heightZ: "SH", color: "\u5BBD\u67F1\u989C\u8272" }]
              }
            ]
          }
        ]
      }
    ]
  }),
  "B:\u6A2A\u5411": check({
    unit: { \u95F4\u8DDD: 8, \u697C\u677F\u51FA\u6311: 1.5, \u697C\u677F\u539A: 3, \u9876\u90E8\u4FEE\u6B63: 0 },
    color: {},
    floor: [
      {
        ctrlFloor: { indent: { end: "\u9876\u90E8\u4FEE\u6B63" } },
        edge: [{ offset: "-\u697C\u677F\u51FA\u6311" }],
        extrude: [{ height: "-\u697C\u677F\u539A", trans: [{ moveZ: "FH+0.5\u697C\u677F\u539A" }] }]
      },
      {
        ctrlFloor: { total: 1 },
        extrude: [{ height: "SH", color: "G" }],
        vertical: [
          { array: [{ space: "\u95F4\u8DDD", boxes: [{ widthX: 2, depthY: 2, heightZ: "SH" }] }] }
        ]
      }
    ]
  }),
  "B:\u7AD6\u5411": check({
    unit: { \u67F1\u5BBD: 3, \u95F4\u8DDD: 8, \u697C\u677F\u7F29\u8FDB: -1, \u697C\u677F\u539A: 1.2, \u964D\u4F4E: 0 },
    color: { \u95E8: COLOR.METAL },
    floor: [
      {
        ctrlFloor: { indent: { start: 1 } },
        edge: [{ offset: "\u697C\u677F\u7F29\u8FDB" }],
        extrude: [{ height: "-\u697C\u677F\u539A" }]
      },
      {
        ctrlFloor: { total: 1 },
        extrude: [{ height: "SH-\u964D\u4F4E", color: "G" }],
        vertical: [
          {
            ctrlArray: { chance: 1 },
            array: [
              {
                space: "\u95F4\u8DDD",
                boxes: [{ widthX: "\u67F1\u5BBD", depthY: "\u67F1\u5BBD", heightZ: "SH-\u964D\u4F4E" }]
              }
            ]
          }
        ]
      }
    ]
  }),
  "B:\u6838\u5FC3\u7B52": check({
    unit: { \u7F29\u8FDB: 6, \u9AD8\u5EA6\u4FEE\u6B63: 3 },
    color: { \u989C\u8272: COLOR.CONCRETE },
    floor: [
      {
        ctrlFloor: { total: 1 },
        edge: [{ offset: "\u7F29\u8FDB" }],
        extrude: [{ height: "H+\u9AD8\u5EA6\u4FEE\u6B63", color: "\u989C\u8272" }]
      }
    ]
  })
};

// src/styles/simple.ts
var styles = {
  SV0: {
    tags: { orient: "V" },
    type: "FREE",
    section: {
      roof: {
        floor: [
          {
            presets: [preset(simple["T:\u5973\u513F\u5899"])]
          }
        ]
      },
      bottom: {
        height: "1H",
        floor: [
          {
            presets: [
              preset(simple["M:\u7AD6\u5411"], { unit: { \u5E95\u90E8\u4FEE\u6B63: 2 } }),
              preset(simple["B:\u6838\u5FC3\u7B52"])
            ]
          }
        ]
      }
    }
  },
  SV11: {
    tags: { orient: "V" },
    type: "FREE",
    section: {
      roof: {
        floor: [
          {
            presets: [preset(simple["T:\u5973\u513F\u5899"], { unit: { \u9AD8\u5EA6: 2 } })]
          }
        ]
      },
      middle: {
        floor: [
          {
            presets: [preset(simple["M:\u7AD6\u5411\u8FDB\u6DF1\u5BF9\u9F50"])]
          }
        ]
      },
      bottom: {
        height: "0.2H",
        floorHeight: 5,
        floor: [
          {
            edge: [{ offset: 1 }],
            presets: [preset(simple["B:\u6A2A\u5411"]), preset(simple["B:\u6838\u5FC3\u7B52"])]
          }
        ]
      }
    }
  },
  SV1: {
    tags: { orient: "V" },
    type: "FREE",
    section: {
      roof: {
        floor: [
          {
            presets: [preset(simple["T:\u5973\u513F\u5899"], { unit: { \u9AD8\u5EA6: 2 } })]
          }
        ]
      },
      middle: {
        floor: [{ presets: [preset(simple["M:\u7AD6\u5411"], { unit: { \u5E95\u90E8\u4FEE\u6B63: 1 } })] }]
      },
      bottom: {
        height: "0.2H",
        floorHeight: 5,
        floor: [
          {
            edge: [{ offset: 1 }],
            presets: [preset(simple["B:\u6A2A\u5411"]), preset(simple["B:\u6838\u5FC3\u7B52"])]
          }
        ]
      }
    }
  },
  SV2: {
    tags: { orient: "V" },
    type: "FREE",
    section: {
      roof: {
        floor: [
          {
            presets: [preset(simple["T:\u5973\u513F\u5899"], { unit: { \u9AD8\u5EA6: 2 } })]
          }
        ]
      },
      middle: {
        floor: [{ presets: [preset(simple["M:\u7AD6\u5411"])] }]
      },
      bottom: {
        height: "0.2H",
        floorHeight: 5,
        floor: [
          {
            edge: [{ offset: 1 }],
            presets: [preset(simple["B:\u7AD6\u5411"]), preset(simple["B:\u6838\u5FC3\u7B52"])]
          }
        ]
      }
    }
  },
  SL0: {
    tags: { orient: "V" },
    type: "FREE",
    section: {
      roof: {
        floor: [
          {
            edge: [{ offset: 1 }],
            presets: [preset(simple["T:\u5973\u513F\u5899"], { unit: { \u62AC\u5347: 0.5 } })]
          }
        ]
      },
      bottom: {
        height: "1H",
        floor: [
          {
            edge: [{ offset: 1 }],
            presets: [
              preset(simple["M:\u6A2A\u5411"], { unit: { \u5E95\u90E8\u4FEE\u6B63: 2 } }),
              preset(simple["B:\u6838\u5FC3\u7B52"])
            ]
          }
        ]
      }
    }
  },
  SL1: {
    tags: { orient: "V" },
    type: "FREE",
    section: {
      roof: {
        floor: [
          {
            presets: [preset(simple["T:\u5973\u513F\u5899"], { unit: { \u9AD8\u5EA6: 2 } })]
          }
        ]
      },
      middle: {
        floor: [{ presets: [preset(simple["M:\u6A2A\u5411"], { unit: { \u5E95\u90E8\u4FEE\u6B63: 1 } })] }]
      },
      bottom: {
        height: "0.2H",
        floorHeight: 5,
        floor: [
          {
            presets: [preset(simple["B:\u6A2A\u5411"]), preset(simple["B:\u6838\u5FC3\u7B52"])]
          }
        ]
      }
    }
  },
  SL2: {
    tags: { orient: "V" },
    type: "FREE",
    section: {
      roof: {
        floor: [
          {
            presets: [preset(simple["T:\u5973\u513F\u5899"], { unit: { \u9AD8\u5EA6: 2 } })]
          }
        ]
      },
      middle: {
        floor: [{ presets: [preset(simple["M:\u6A2A\u5411"])] }]
      },
      bottom: {
        height: "0.2H",
        floorHeight: 5,
        floor: [
          {
            edge: [{ offset: 1 }],
            presets: [preset(simple["B:\u7AD6\u5411"]), preset(simple["B:\u6838\u5FC3\u7B52"])]
          }
        ]
      }
    }
  }
};

// bundle/esbuild_magiz.ts
var styles2 = new MagizStyles([styles]);
export {
  Plan,
  styles2 as styles
};
/*!
  Magiz
  light weight 3D building generator for web
  Copyright (c) 2019-2024 周曦 <453154007@qq.com>
  http://www.architech.fun
  GPL Version 3 license
*/
//# sourceMappingURL=magiz.module.min.js.map
